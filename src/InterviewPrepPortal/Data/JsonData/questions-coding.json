{
  "categoryId": "coding",
  "questions": [
    {
      "id": "coding-001",
      "category": "coding",
      "difficulty": "easy",
      "frequency": "very-high",
      "question": "Reverse a string in C# — multiple approaches",
      "answer": "<pre><code class=\"language-csharp\">// Method 1: LINQ (cleanest for interviews)\nstring Reverse1(string s) =>\n    new string(s.Reverse().ToArray());\n\n// Method 2: Array.Reverse (most readable)\nstring Reverse2(string s)\n{\n    var chars = s.ToCharArray();\n    Array.Reverse(chars);\n    return new string(chars);\n}\n\n// Method 3: StringBuilder (manual, shows understanding)\nstring Reverse3(string s)\n{\n    var sb = new StringBuilder(s.Length);\n    for (int i = s.Length - 1; i >= 0; i--)\n        sb.Append(s[i]);\n    return sb.ToString();\n}\n\n// Method 4: Two-pointer in-place on char array\nstring Reverse4(string s)\n{\n    var chars = s.ToCharArray();\n    int left = 0, right = chars.Length - 1;\n    while (left &lt; right)\n    {\n        (chars[left], chars[right]) = (chars[right], chars[left]);\n        left++; right--;\n    }\n    return new string(chars);\n}\n\n// Edge cases to mention:\n// - null/empty string → return as-is\n// - Unicode surrogate pairs (emoji) break char-level reversal\n// - For Unicode safety: use StringInfo.GetTextElementEnumerator()</code></pre><p>Time: O(n) for all methods. Space: O(n) for all (strings are immutable in C#, always need a new string).</p>",
      "followUps": ["How do you reverse a string without using built-in methods?", "How do you reverse words in a sentence?", "What about Unicode/emoji characters?"],
      "tip": "<strong>Interview answer:</strong> Show Method 1 (LINQ) as the production approach, then Method 4 (two-pointer) to show you understand the algorithm. Mention the Unicode edge case — it shows senior-level thinking.",
      "tags": ["coding", "string", "algorithm"],
      "sortOrder": 1,
      "isActive": true
    },
    {
      "id": "coding-002",
      "category": "coding",
      "difficulty": "easy",
      "frequency": "very-high",
      "question": "Check if a string is a palindrome",
      "answer": "<pre><code class=\"language-csharp\">// Method 1: Two-pointer (most efficient)\nbool IsPalindrome(string s)\n{\n    int left = 0, right = s.Length - 1;\n    while (left &lt; right)\n    {\n        if (s[left] != s[right]) return false;\n        left++;\n        right--;\n    }\n    return true;\n}\n\n// Method 2: Compare with reversed (simple but creates extra string)\nbool IsPalindrome2(string s) =>\n    s == new string(s.Reverse().ToArray());\n\n// Variant: ignore case, non-alphanumeric (common follow-up)\nbool IsPalindromeAlphanumeric(string s)\n{\n    var clean = s.Where(char.IsLetterOrDigit)\n                 .Select(char.ToLower)\n                 .ToArray();\n    int l = 0, r = clean.Length - 1;\n    while (l &lt; r)\n    {\n        if (clean[l] != clean[r]) return false;\n        l++; r--;\n    }\n    return true;\n}\n\n// Examples:\n// IsPalindrome(\"racecar\") → true\n// IsPalindrome(\"hello\") → false\n// IsPalindromeAlphanumeric(\"A man a plan a canal Panama\") → true</code></pre><p>Time: O(n). Space: O(1) for two-pointer (O(n) if you create the reversed string).</p>",
      "followUps": ["What is the longest palindromic substring?", "How do you check if a number is a palindrome?"],
      "tip": "<strong>For numbers:</strong> Convert to string and use the same approach, OR reverse mathematically (no string conversion): extract digits via modulo and rebuild in reverse. Mention both.",
      "tags": ["coding", "string", "algorithm", "two-pointer"],
      "sortOrder": 2,
      "isActive": true
    },
    {
      "id": "coding-003",
      "category": "coding",
      "difficulty": "easy",
      "frequency": "high",
      "question": "Find the factorial of a number — recursive and iterative",
      "answer": "<pre><code class=\"language-csharp\">// Iterative (preferred — no stack overflow risk)\nlong Factorial(int n)\n{\n    if (n &lt; 0) throw new ArgumentException(\"n must be non-negative\");\n    long result = 1;\n    for (int i = 2; i &lt;= n; i++)\n        result *= i;\n    return result;\n}\n\n// Recursive (elegant but stack overflow risk for large n)\nlong FactorialRecursive(int n)\n{\n    if (n &lt; 0) throw new ArgumentException(\"n must be non-negative\");\n    if (n &lt;= 1) return 1;  // base case\n    return n * FactorialRecursive(n - 1);\n}\n\n// With memoization (if calling frequently with same values)\nprivate static readonly Dictionary&lt;int, long&gt; _cache = new();\nlong FactorialMemo(int n)\n{\n    if (n &lt;= 1) return 1;\n    if (_cache.TryGetValue(n, out var cached)) return cached;\n    var result = n * FactorialMemo(n - 1);\n    _cache[n] = result;\n    return result;\n}\n\n// Note: long overflows at 21! (use BigInteger for large n)\n// using System.Numerics;\nBigInteger FactorialBig(int n)\n{\n    BigInteger result = 1;\n    for (int i = 2; i &lt;= n; i++) result *= i;\n    return result;\n}</code></pre>",
      "followUps": ["What's the maximum factorial that fits in a long?", "How would you compute factorial of very large numbers?"],
      "tip": "<strong>20! = 2,432,902,008,176,640,000</strong> (fits in long). <strong>21!</strong> overflows long. Mention this — it shows awareness of integer overflow, a common production bug.",
      "tags": ["coding", "algorithm", "recursion", "math"],
      "sortOrder": 3,
      "isActive": true
    },
    {
      "id": "coding-004",
      "category": "coding",
      "difficulty": "easy",
      "frequency": "high",
      "question": "Find the Fibonacci sequence — iterative, recursive, and dynamic programming",
      "answer": "<pre><code class=\"language-csharp\">// Naive recursive — O(2^n), terrible for large n\nlong FibRecursive(int n)\n{\n    if (n &lt;= 1) return n;\n    return FibRecursive(n - 1) + FibRecursive(n - 2);\n}\n\n// Iterative — O(n) time, O(1) space (best for interviews)\nlong FibIterative(int n)\n{\n    if (n &lt;= 1) return n;\n    long prev = 0, curr = 1;\n    for (int i = 2; i &lt;= n; i++)\n    {\n        long next = prev + curr;\n        prev = curr;\n        curr = next;\n    }\n    return curr;\n}\n\n// Memoization — O(n) time, O(n) space\nlong FibMemo(int n, Dictionary&lt;int, long&gt;? memo = null)\n{\n    memo ??= new Dictionary&lt;int, long&gt;();\n    if (n &lt;= 1) return n;\n    if (memo.TryGetValue(n, out var cached)) return cached;\n    var result = FibMemo(n - 1, memo) + FibMemo(n - 2, memo);\n    memo[n] = result;\n    return result;\n}\n\n// Using IEnumerable to stream the sequence\nIEnumerable&lt;long&gt; FibSequence()\n{\n    long prev = 0, curr = 1;\n    yield return prev;\n    yield return curr;\n    while (true)\n    {\n        long next = prev + curr;\n        yield return next;\n        prev = curr;\n        curr = next;\n    }\n}</code></pre>",
      "followUps": ["What is the time complexity of each approach?", "How do you get the first N Fibonacci numbers?"],
      "tip": "<strong>Recursive Fibonacci is O(2^n)</strong> — interview interviewers ask it to see if you know to use memoization/iteration instead. Always show the iterative approach as your preferred solution, then explain memoization as a way to add caching to the recursive version.",
      "tags": ["coding", "algorithm", "recursion", "dynamic-programming"],
      "sortOrder": 4,
      "isActive": true
    },
    {
      "id": "coding-005",
      "category": "coding",
      "difficulty": "medium",
      "frequency": "very-high",
      "question": "Find duplicates in an array",
      "answer": "<pre><code class=\"language-csharp\">// Method 1: HashSet — O(n) time, O(n) space\nIEnumerable&lt;int&gt; FindDuplicates(int[] arr)\n{\n    var seen = new HashSet&lt;int&gt;();\n    var duplicates = new HashSet&lt;int&gt;();\n    foreach (var num in arr)\n    {\n        if (!seen.Add(num))     // Add returns false if already present\n            duplicates.Add(num);\n    }\n    return duplicates;\n}\n\n// Method 2: LINQ GroupBy (concise)\nIEnumerable&lt;int&gt; FindDuplicatesLinq(int[] arr) =>\n    arr.GroupBy(x => x)\n       .Where(g => g.Count() &gt; 1)\n       .Select(g => g.Key);\n\n// Method 3: Sort + adjacent comparison — O(n log n), O(1) extra space\nIEnumerable&lt;int&gt; FindDuplicatesSorted(int[] arr)\n{\n    Array.Sort(arr);\n    var result = new List&lt;int&gt;();\n    for (int i = 1; i &lt; arr.Length; i++)\n        if (arr[i] == arr[i-1] &amp;&amp; (result.Count == 0 || result[^1] != arr[i]))\n            result.Add(arr[i]);\n    return result;\n}\n\n// Variant: find all elements that appear more than once, with count\nDictionary&lt;int, int&gt; CountFrequency(int[] arr) =>\n    arr.GroupBy(x => x)\n       .ToDictionary(g => g.Key, g => g.Count());\n\n// Example:\n// Input:  [1, 3, 4, 2, 2, 3]\n// Output: [2, 3]</code></pre>",
      "followUps": ["What if elements are in range 1..n? (cycle detection)", "Find the only duplicate in an array of 1..n"],
      "tip": "<strong>If values are in range 1..n:</strong> Floyd's cycle detection or XOR trick can solve in O(n) time and O(1) space — but these are advanced. For most interviews, the HashSet approach is expected and optimal enough.",
      "tags": ["coding", "array", "hashset", "algorithm"],
      "sortOrder": 5,
      "isActive": true
    },
    {
      "id": "coding-006",
      "category": "coding",
      "difficulty": "medium",
      "frequency": "high",
      "question": "Find the two numbers in an array that sum to a target (Two Sum)",
      "answer": "<pre><code class=\"language-csharp\">// O(n) — HashMap approach (optimal)\nint[] TwoSum(int[] nums, int target)\n{\n    var seen = new Dictionary&lt;int, int&gt;();  // value → index\n    for (int i = 0; i &lt; nums.Length; i++)\n    {\n        int complement = target - nums[i];\n        if (seen.TryGetValue(complement, out int j))\n            return [j, i];  // found\n        seen[nums[i]] = i;\n    }\n    throw new ArgumentException(\"No solution\");\n}\n\n// O(n log n) — Two pointer (if array is sorted, or if you can sort it)\nint[] TwoSumSorted(int[] sortedNums, int target)\n{\n    int left = 0, right = sortedNums.Length - 1;\n    while (left &lt; right)\n    {\n        int sum = sortedNums[left] + sortedNums[right];\n        if (sum == target)  return [left, right];\n        if (sum &lt; target)   left++;\n        else                right--;\n    }\n    throw new ArgumentException(\"No solution\");\n}\n\n// O(n²) — Brute force (mention but don't use)\nint[] TwoSumBrute(int[] nums, int target)\n{\n    for (int i = 0; i &lt; nums.Length; i++)\n        for (int j = i + 1; j &lt; nums.Length; j++)\n            if (nums[i] + nums[j] == target)\n                return [i, j];\n    throw new ArgumentException(\"No solution\");\n}\n\n// Example: nums = [2, 7, 11, 15], target = 9 → [0, 1]</code></pre>",
      "followUps": ["What if there are multiple pairs?", "What if the array is sorted?", "What about Three Sum?"],
      "tip": "<strong>Walk through the HashMap approach step by step:</strong> for each element, compute what complement you need, check if you've seen it. This one-pass O(n) approach is the canonical answer. The interviewer is testing if you know when to trade space for time.",
      "tags": ["coding", "array", "hashmap", "two-pointer", "algorithm"],
      "sortOrder": 6,
      "isActive": true
    },
    {
      "id": "coding-007",
      "category": "coding",
      "difficulty": "medium",
      "frequency": "high",
      "question": "Remove duplicates from a sorted array in-place",
      "answer": "<pre><code class=\"language-csharp\">// O(n) time, O(1) space — two-pointer in-place\nint RemoveDuplicates(int[] nums)\n{\n    if (nums.Length == 0) return 0;\n\n    int uniqueCount = 1;  // first element is always unique\n    for (int i = 1; i &lt; nums.Length; i++)\n    {\n        if (nums[i] != nums[i - 1])  // found a new unique value\n        {\n            nums[uniqueCount] = nums[i];  // place it at next unique position\n            uniqueCount++;\n        }\n    }\n    return uniqueCount;  // first uniqueCount elements are the deduplicated array\n}\n\n// Example:\n// Input:  [1, 1, 2, 3, 3, 4]\n// After:  [1, 2, 3, 4, _, _]  (first 4 elements matter)\n// Return: 4\n\n// LINQ alternative (creates new array, O(n) space)\nint[] RemoveDuplicatesLinq(int[] sorted) =>\n    sorted.Distinct().ToArray();  // works on unsorted too (uses HashSet internally)\n\n// For unsorted array with distinct values preserved:\nint[] RemoveDuplicatesUnsorted(int[] nums)\n{\n    var seen = new HashSet&lt;int&gt;();\n    return nums.Where(seen.Add).ToArray();  // seen.Add returns false for duplicates\n}</code></pre>",
      "followUps": ["What if the array allows each element at most twice?", "What about removing duplicates from an unsorted array?"],
      "tip": "<strong>Slow-fast pointer pattern:</strong> slow pointer (uniqueCount) tracks where to write next unique element. Fast pointer (i) scans the array. This O(1) space pattern applies to many array modification problems.",
      "tags": ["coding", "array", "two-pointer", "algorithm"],
      "sortOrder": 7,
      "isActive": true
    },
    {
      "id": "coding-008",
      "category": "coding",
      "difficulty": "medium",
      "frequency": "high",
      "question": "Find the maximum subarray sum (Kadane's Algorithm)",
      "answer": "<pre><code class=\"language-csharp\">// Kadane's Algorithm — O(n) time, O(1) space\nint MaxSubarraySum(int[] nums)\n{\n    if (nums.Length == 0) throw new ArgumentException(\"Empty array\");\n\n    int maxSum = nums[0];\n    int currentSum = nums[0];\n\n    for (int i = 1; i &lt; nums.Length; i++)\n    {\n        // Either extend current subarray or start fresh from nums[i]\n        currentSum = Math.Max(nums[i], currentSum + nums[i]);\n        maxSum = Math.Max(maxSum, currentSum);\n    }\n    return maxSum;\n}\n\n// Variant: also return the subarray indices\n(int maxSum, int start, int end) MaxSubarrayWithIndices(int[] nums)\n{\n    int maxSum = nums[0], currentSum = nums[0];\n    int maxStart = 0, maxEnd = 0, tempStart = 0;\n\n    for (int i = 1; i &lt; nums.Length; i++)\n    {\n        if (nums[i] &gt; currentSum + nums[i])\n        {\n            currentSum = nums[i];\n            tempStart = i;\n        }\n        else\n        {\n            currentSum += nums[i];\n        }\n        if (currentSum &gt; maxSum)\n        {\n            maxSum = currentSum;\n            maxStart = tempStart;\n            maxEnd = i;\n        }\n    }\n    return (maxSum, maxStart, maxEnd);\n}\n\n// Example:\n// [-2, 1, -3, 4, -1, 2, 1, -5, 4] → 6 (subarray [4, -1, 2, 1])</code></pre>",
      "followUps": ["What if the array is all negative?", "What is the circular maximum subarray?"],
      "tip": "<strong>Kadane's key insight:</strong> at each position, you decide to either extend the previous subarray or start a new one from the current element. Whichever is larger becomes the current sum. Keep track of the global maximum.",
      "tags": ["coding", "array", "dynamic-programming", "algorithm"],
      "sortOrder": 8,
      "isActive": true
    },
    {
      "id": "coding-009",
      "category": "coding",
      "difficulty": "medium",
      "frequency": "medium",
      "question": "Implement a stack using a queue and a queue using a stack",
      "answer": "<pre><code class=\"language-csharp\">// Stack using two queues — push O(n), pop O(1)\npublic class StackUsingQueues&lt;T&gt;\n{\n    private Queue&lt;T&gt; _q1 = new();\n    private Queue&lt;T&gt; _q2 = new();\n\n    public void Push(T item)\n    {\n        _q2.Enqueue(item);               // enqueue to empty q2\n        while (_q1.Count &gt; 0)           // move all q1 to q2\n            _q2.Enqueue(_q1.Dequeue());\n        (_q1, _q2) = (_q2, _q1);        // swap references (q1 is now the stack)\n    }\n\n    public T Pop()  => _q1.Dequeue();\n    public T Peek() => _q1.Peek();\n    public bool IsEmpty => _q1.Count == 0;\n}\n\n// Queue using two stacks — enqueue O(1), dequeue amortized O(1)\npublic class QueueUsingStacks&lt;T&gt;\n{\n    private Stack&lt;T&gt; _inbox  = new();   // new items go here\n    private Stack&lt;T&gt; _outbox = new();   // items dequeued from here\n\n    public void Enqueue(T item) => _inbox.Push(item);\n\n    public T Dequeue()\n    {\n        if (_outbox.Count == 0)           // outbox empty: refill from inbox\n        {\n            while (_inbox.Count &gt; 0)\n                _outbox.Push(_inbox.Pop()); // reverses order = FIFO\n        }\n        return _outbox.Pop();\n    }\n\n    public T Peek()\n    {\n        if (_outbox.Count == 0)\n            while (_inbox.Count &gt; 0)\n                _outbox.Push(_inbox.Pop());\n        return _outbox.Peek();\n    }\n}</code></pre>",
      "followUps": ["What is the amortized complexity of QueueUsingStacks.Dequeue()?", "What is the Min Stack problem?"],
      "tip": "<strong>Amortized O(1) for dequeue:</strong> each element crosses from inbox to outbox at most once. Even though a single dequeue might take O(n) to refill, the average across N operations is O(1). This is the amortized analysis explanation interviewers want.",
      "tags": ["coding", "stack", "queue", "data-structures"],
      "sortOrder": 9,
      "isActive": true
    },
    {
      "id": "coding-010",
      "category": "coding",
      "difficulty": "hard",
      "frequency": "medium",
      "question": "Implement binary search",
      "answer": "<pre><code class=\"language-csharp\">// Iterative — O(log n) time, O(1) space (preferred)\nint BinarySearch(int[] sortedArr, int target)\n{\n    int left = 0, right = sortedArr.Length - 1;\n    while (left &lt;= right)\n    {\n        int mid = left + (right - left) / 2;  // avoid integer overflow vs (left+right)/2\n        if (sortedArr[mid] == target) return mid;\n        if (sortedArr[mid] &lt; target)  left = mid + 1;\n        else                           right = mid - 1;\n    }\n    return -1;  // not found\n}\n\n// Recursive\nint BinarySearchRecursive(int[] arr, int target, int left, int right)\n{\n    if (left &gt; right) return -1;\n    int mid = left + (right - left) / 2;\n    if (arr[mid] == target) return mid;\n    if (arr[mid] &lt; target)  return BinarySearchRecursive(arr, target, mid + 1, right);\n    else                     return BinarySearchRecursive(arr, target, left, mid - 1);\n}\n\n// Find first occurrence of target (lower bound)\nint FirstOccurrence(int[] sorted, int target)\n{\n    int left = 0, right = sorted.Length - 1, result = -1;\n    while (left &lt;= right)\n    {\n        int mid = left + (right - left) / 2;\n        if (sorted[mid] == target) { result = mid; right = mid - 1; }  // continue left\n        else if (sorted[mid] &lt; target) left = mid + 1;\n        else right = mid - 1;\n    }\n    return result;\n}\n\n// .NET built-in: Array.BinarySearch(sortedArray, target)\n// Returns index if found, ~index of next larger element if not found</code></pre>",
      "followUps": ["What is the time complexity?", "How do you search in a rotated sorted array?"],
      "tip": "<strong>mid = left + (right - left) / 2</strong> — not (left + right) / 2. The latter can overflow if left and right are both large ints. This is a classic bug that crashed the Java standard library binary search implementation in 2006. Always mention it.",
      "tags": ["coding", "binary-search", "algorithm", "array"],
      "sortOrder": 10,
      "isActive": true
    }
  ]
}
