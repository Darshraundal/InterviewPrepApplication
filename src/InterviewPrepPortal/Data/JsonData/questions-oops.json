{
  "categoryId": "oops",
  "questions": [
    {
      "id": "oops-001",
      "category": "oops",
      "difficulty": "easy",
      "frequency": "very-high",
      "question": "What are the four pillars of OOP?",
      "answer": "<p>The four pillars are the core principles that define object-oriented programming. I'll explain each with how I apply them in .NET:</p><ul><li><strong>Encapsulation</strong> — bundle data and behavior together, hide internal implementation behind a public interface. In C#: private fields, public properties with getters/setters. The <code>Portfolio</code> class exposes <code>AddHolding()</code> but hides how it validates and adjusts weights internally.</li><li><strong>Inheritance</strong> — a class (child) inherits fields and behaviors from another class (parent). In C#: the <code>: BaseClass</code> syntax. <code>ApplicationUser : IdentityUser</code> inherits all Identity fields while adding custom ones like <code>DisplayName</code>.</li><li><strong>Polymorphism</strong> — the same interface or method behaves differently for different types. In C#: method overriding (<code>virtual/override</code>), method overloading, interfaces.</li><li><strong>Abstraction</strong> — expose only what's necessary, hide complexity. In C#: interfaces and abstract classes define the \"what\", concrete implementations define the \"how\". <code>IEmailSender</code> abstracts SMTP vs SendGrid vs console-logger in tests.</li></ul>",
      "followUps": [
        {
          "question": "What is the difference between abstraction and encapsulation?",
          "answer": "<p><strong>Abstraction</strong> operates at the design level — it hides complexity by exposing only the relevant interface. An <code>IEmailSender</code> interface shows callers just <code>SendAsync(to, subject, body)</code> without revealing whether it uses SMTP, SendGrid, or a fake. <strong>Encapsulation</strong> operates at the implementation level — it bundles data and methods together and restricts direct access to internal state using access modifiers. A <code>BankAccount</code> class encapsulates the <code>_balance</code> field as private and exposes <code>Deposit()</code> and <code>Withdraw()</code> methods that enforce business rules. Abstraction is about what the interface shows; encapsulation is about how the class protects its internals. Both hide information but at different levels.</p>"
        },
        {
          "question": "Can you have OOP without inheritance?",
          "answer": "<p>Yes — inheritance is one pillar but not a requirement for OOP. Many modern .NET designs deliberately avoid class inheritance and favour <strong>composition</strong> and <strong>interfaces</strong> instead. Interfaces provide polymorphism and abstraction without the tight coupling of inheritance. Composition embeds behaviour via injected dependencies rather than inheriting it. Go (the language) has no class inheritance at all, yet is considered object-oriented via interfaces and composition. In C#, the advice \"favour composition over inheritance\" is a widely accepted principle — inherit only when there is a genuine \"is a\" relationship and you want to share implementation. For defining contracts (polymorphism), interfaces are almost always preferable to abstract class inheritance.</p>"
        }
      ],
      "tip": "<strong>Abstraction vs Encapsulation:</strong> Abstraction is about hiding complexity at the design level (\"what it does\"). Encapsulation is about hiding implementation at the code level (\"how it does it\"). Interfaces are abstraction; private fields are encapsulation.",
      "tags": ["oops", "inheritance", "polymorphism", "abstraction", "encapsulation"],
      "sortOrder": 1,
      "isActive": true
    },
    {
      "id": "oops-002",
      "category": "oops",
      "difficulty": "medium",
      "frequency": "very-high",
      "question": "What is the difference between an interface and an abstract class?",
      "answer": "<p>Both define a contract that implementing classes must fulfill, but they have key differences:</p><table style=\"width:100%;font-size:.85rem;border-collapse:collapse;\"><tr><th>Feature</th><th>Interface</th><th>Abstract Class</th></tr><tr><td>Instantiation</td><td>Cannot instantiate</td><td>Cannot instantiate</td></tr><tr><td>Multiple inheritance</td><td>Class can implement many</td><td>Class can extend only one</td></tr><tr><td>State (fields)</td><td>No instance fields (C# 8: static fields only)</td><td>Can have instance fields</td></tr><tr><td>Default implementations</td><td>Yes (C# 8+)</td><td>Yes (non-abstract methods)</td></tr><tr><td>Constructor</td><td>No</td><td>Yes (called by derived class)</td></tr></table><p><strong>When I use what:</strong></p><ul><li><strong>Interface</strong> — for defining capabilities that unrelated classes can share. <code>IEmailSender</code>, <code>IProgressService</code> — any class can implement it regardless of its hierarchy. Perfect for DI and mocking.</li><li><strong>Abstract class</strong> — for sharing code among closely related classes. <code>BasePaymentProcessor</code> with shared validation logic, leaving only <code>ProcessPaymentAsync()</code> abstract for subclasses to implement.</li></ul><p>In the CSInvoice project, <code>IDocumentGenerate</code> is an interface because unrelated invoice types (Normal, CP, Monthly) all implement the same contract but share no common code.</p>",
      "followUps": [
        {
          "question": "When would you choose abstract class over interface?",
          "answer": "<p>Choose an <strong>abstract class</strong> when: (1) You want to share <em>implementation</em> (method bodies, fields) among closely related classes — not just the contract. A <code>BaseReportGenerator</code> that handles file I/O and logging, leaving only <code>BuildContent()</code> abstract for subclasses. (2) You need a constructor to enforce initialization. (3) The Template Method pattern — the base class defines the algorithm skeleton and subclasses fill in specific steps. Choose an <strong>interface</strong> when: you're defining a capability that unrelated types can implement, you need to support multiple inheritance of type, or you want maximum flexibility for DI/mocking. In modern C# (8+), interfaces can have default implementations, blurring the line — but abstract classes are still the cleaner choice when sharing state or constructor logic.</p>"
        },
        {
          "question": "What are default interface methods in C# 8?",
          "answer": "<p>C# 8 introduced <strong>default interface methods</strong> — interface members can now have a body (default implementation). Classes implementing the interface automatically get the default implementation unless they override it. This was added to allow interface evolution without breaking existing implementers: add a new method with a default implementation, and classes that don't override it continue to compile. Example: <code>public interface ILogger { void Log(string msg); void LogError(string msg) => Log($\"[ERROR] {msg}\"); }</code>. Classes implementing <code>ILogger</code> get <code>LogError</code> for free. Caution: default methods are only accessible through the interface reference — casting to the concrete type won't expose them. Use sparingly; abstract classes are cleaner for sharing implementation.</p>"
        }
      ],
      "tip": "<strong>Prefer interfaces over abstract classes</strong> for .NET dependency injection — you can only mock/substitute interfaces cleanly. Abstract classes are fine for template method pattern where you're sharing implementation, not just a contract.",
      "tags": ["oops", "interface", "abstract-class"],
      "sortOrder": 2,
      "isActive": true
    },
    {
      "id": "oops-003",
      "category": "oops",
      "difficulty": "medium",
      "frequency": "very-high",
      "question": "What is polymorphism and what are its types in C#?",
      "answer": "<p><strong>Polymorphism</strong> means \"many forms\" — the ability to treat different types through a common interface. Two main types in C#:</p><p><strong>1. Compile-time (static) polymorphism — method overloading:</strong></p><pre><code class=\"language-csharp\">public class EmailSender\n{\n    public void Send(string to, string subject) { ... }\n    public void Send(string to, string subject, string template) { ... }\n    // Same name, different parameter signatures — resolved at compile time\n}</code></pre><p><strong>2. Runtime (dynamic) polymorphism — method overriding via virtual/override:</strong></p><pre><code class=\"language-csharp\">public abstract class NotificationSender\n{\n    public abstract Task SendAsync(Notification n);\n}\n\npublic class EmailNotificationSender : NotificationSender\n{\n    public override async Task SendAsync(Notification n) { /* SMTP logic */ }\n}\n\npublic class SmsNotificationSender : NotificationSender\n{\n    public override async Task SendAsync(Notification n) { /* SMS logic */ }\n}\n\n// Polymorphic usage — correct Send() called at runtime\nNotificationSender sender = GetSender(notificationType);\nawait sender.SendAsync(notification);</code></pre>",
      "followUps": [
        {
          "question": "What is method hiding vs method overriding?",
          "answer": "<p><strong>Method overriding</strong> (<code>virtual</code> + <code>override</code>) — the derived class replaces the base class method. Virtual dispatch ensures the most derived implementation is called, even when accessed through a base class reference. This is true polymorphism. <strong>Method hiding</strong> (<code>new</code> keyword) — the derived class hides the base method with a new one. When accessed through a base reference, the <em>base</em> method is called; only when accessed through the derived reference is the new method called. Example: if <code>Dog</code> hides <code>Animal.Speak()</code> with <code>new string Speak()</code>, then <code>Animal a = new Dog(); a.Speak()</code> calls the <em>Animal</em> version — not the Dog version. This is rarely what you want. Always use <code>override</code> for polymorphism; use <code>new</code> only when intentionally breaking the polymorphic chain.</p>"
        },
        {
          "question": "What is operator overloading?",
          "answer": "<p><strong>Operator overloading</strong> allows you to define how standard operators (<code>+</code>, <code>-</code>, <code>==</code>, <code>&gt;</code>, etc.) behave for your custom types. Define a <code>public static</code> method named <code>operator +</code>: <code>public static Money operator +(Money a, Money b) => new Money(a.Amount + b.Amount, a.Currency);</code>. Now <code>price1 + price2</code> works naturally for your <code>Money</code> type. Common overloads: <code>==</code> and <code>!=</code> (always override both together and override <code>Equals</code>/<code>GetHashCode</code> to match), <code>+</code> for string-like types, <code>&gt;</code>/<code>&lt;</code> for comparable value objects. Overuse of operator overloading can make code confusing — only overload when the semantics are obvious from the operator symbol.</p>"
        }
      ],
      "tip": "<strong>Method hiding vs overriding:</strong> <code>override</code> replaces the base method (virtual dispatch). <code>new</code> hides the base method — calling through the base type still calls the base method. Always prefer <code>override</code>; use <code>new</code> only intentionally.",
      "tags": ["oops", "polymorphism", "overloading", "overriding"],
      "sortOrder": 3,
      "isActive": true
    },
    {
      "id": "oops-004",
      "category": "oops",
      "difficulty": "medium",
      "frequency": "high",
      "question": "What is encapsulation and how do you implement it in C#?",
      "answer": "<p><strong>Encapsulation</strong> means bundling data and the methods that operate on it, and restricting direct access to internal state. The class exposes a clean public API while hiding implementation details.</p><p><strong>C# access modifiers:</strong></p><ul><li><code>public</code> — accessible from anywhere</li><li><code>private</code> — accessible only within the class</li><li><code>protected</code> — accessible within class and derived classes</li><li><code>internal</code> — accessible within the same assembly</li><li><code>private protected</code> — derived classes in same assembly only</li></ul><pre><code class=\"language-csharp\">public class Portfolio\n{\n    private decimal _totalValue;\n    private readonly List&lt;Holding&gt; _holdings = [];\n\n    public decimal TotalValue =&gt; _totalValue;  // read-only exposure\n    public IReadOnlyList&lt;Holding&gt; Holdings =&gt; _holdings.AsReadOnly();\n\n    public void AddHolding(Holding holding)\n    {\n        ArgumentNullException.ThrowIfNull(holding);\n        if (holding.Quantity &lt;= 0)\n            throw new ArgumentException(\"Quantity must be positive\");\n        _holdings.Add(holding);\n        RecalculateTotalValue();\n    }\n\n    private void RecalculateTotalValue()  // internal implementation hidden\n    {\n        _totalValue = _holdings.Sum(h =&gt; h.Quantity * h.CurrentPrice);\n    }\n}</code></pre>",
      "followUps": [
        {
          "question": "What is the difference between a field and a property?",
          "answer": "<p>A <strong>field</strong> is a variable declared directly in a class: <code>private int _count;</code>. Direct data storage, no logic possible. A <strong>property</strong> is a member with a getter and/or setter that can contain logic: <code>public int Count { get => _count; set { if (value &lt; 0) throw new ArgumentException(); _count = value; } }</code>. Properties are the standard way to expose data from a class — they allow validation in the setter, computed values in the getter, and the flexibility to change the backing store later without breaking callers. In C#, auto-properties (<code>public int Count { get; set; }</code>) are syntactic sugar — the compiler generates a backing field automatically. Always expose data via properties, never via public fields.</p>"
        },
        {
          "question": "What is a readonly field vs a const?",
          "answer": "<p>A <strong><code>const</code></strong> field is a compile-time constant — its value is baked into the calling assembly's IL at compile time. It can only hold primitive types or strings. Changes require recompiling all dependent assemblies. A <strong><code>readonly</code></strong> field is set once — in the declaration or the constructor — and cannot be changed after. Its value is determined at runtime, so it can hold complex types, calculated values, or values from configuration. Key difference: <code>const string Version = \"1.0\"</code> — if you change it and ship a new DLL, dependent assemblies that haven't recompiled still have the old value baked in. <code>static readonly string Version = GetVersion()</code> — always reads the current value at runtime. For public API values that might change, always prefer <code>static readonly</code> over <code>const</code>.</p>"
        }
      ],
      "tip": "<strong>Expose collections as IReadOnlyList&lt;T&gt;</strong> — returning the internal <code>List&lt;T&gt;</code> directly lets callers add/remove items bypassing your validation. Return <code>AsReadOnly()</code> or the <code>IReadOnlyList&lt;T&gt;</code> interface.",
      "tags": ["oops", "encapsulation", "access-modifiers"],
      "sortOrder": 4,
      "isActive": true
    },
    {
      "id": "oops-005",
      "category": "oops",
      "difficulty": "medium",
      "frequency": "high",
      "question": "What is inheritance in C# and what are its limitations?",
      "answer": "<p>Inheritance lets a derived class acquire the members (fields, properties, methods) of a base class and extend or override them.</p><pre><code class=\"language-csharp\">public class Animal\n{\n    public string Name { get; set; } = string.Empty;\n    public virtual string Speak() =&gt; \"...\";\n}\n\npublic class Dog : Animal\n{\n    public override string Speak() =&gt; \"Woof!\";\n}\n\npublic sealed class Labrador : Dog  // sealed: cannot be further derived\n{\n    public override string Speak() =&gt; \"Woof woof!\";\n}</code></pre><p><strong>Limitations of inheritance:</strong></p><ul><li><strong>Single inheritance only</strong> — C# classes can only extend one class (use interfaces for multiple contracts).</li><li><strong>Tight coupling</strong> — changes to the base class ripple to all derived classes. Fragile base class problem.</li><li><strong>Can't inherit from sealed class</strong> — <code>sealed</code> prevents further derivation (e.g., <code>String</code> is sealed).</li><li><strong>Inheritance is overused</strong> — \"Favour composition over inheritance\" is a core design principle.</li></ul>",
      "followUps": [
        {
          "question": "What is the fragile base class problem?",
          "answer": "<p>The <strong>fragile base class problem</strong> is when changes to a base class unexpectedly break derived classes — even when the change looks safe. Example: a base class adds a new virtual method with a default implementation; a derived class already has a method with the same name (hiding, not overriding it) — now the behaviour changes silently. Or: the base class changes the order of virtual method calls in an overridable template method — derived class overrides that relied on the old order now misbehave. This tight coupling is why many designers prefer composition: a composed dependency can be changed or replaced without affecting the consumer, whereas base class changes can silently break subclasses. It's one of the key reasons \"favour composition over inheritance\" is a core OOP principle.</p>"
        },
        {
          "question": "Composition vs inheritance — when to use which?",
          "answer": "<p>Use <strong>inheritance</strong> when there is a genuine \"is a\" relationship AND you want to share implementation across a hierarchy of related classes: <code>Dog IS AN Animal</code>, <code>AdminUser IS A User</code>. Use <strong>composition</strong> when you want to add behaviour by embedding objects: <code>Car HAS AN Engine</code>. Composition is more flexible — you can swap the composed object at runtime, test the composing class independently, and change behaviour without touching the class hierarchy. The practical test: if you find yourself inheriting just to reuse a method (not because of an \"is a\" relationship), prefer composition. In .NET DI, composition is natural — services are injected rather than inherited. Most production .NET code uses interfaces + composition; inheritance is reserved for genuinely hierarchical domain models.</p>"
        }
      ],
      "tip": "<strong>Composition over Inheritance rule of thumb:</strong> Ask \"is a\" vs \"has a\". <code>Dog IS AN Animal</code> → inheritance. <code>Car HAS AN Engine</code> → composition. If in doubt, compose — it's easier to change later.",
      "tags": ["oops", "inheritance", "composition"],
      "sortOrder": 5,
      "isActive": true
    },
    {
      "id": "oops-006",
      "category": "oops",
      "difficulty": "medium",
      "frequency": "high",
      "question": "What are generics in C# and why are they useful?",
      "answer": "<p><strong>Generics</strong> allow you to write type-safe, reusable code without specifying the actual type until the code is used. The compiler enforces type safety at compile time (no boxing/unboxing, no casting).</p><pre><code class=\"language-csharp\">// Generic repository — works for any entity type\npublic interface IRepository&lt;T&gt; where T : class\n{\n    Task&lt;T?&gt; GetByIdAsync(int id);\n    Task&lt;IReadOnlyList&lt;T&gt;&gt; GetAllAsync();\n    Task AddAsync(T entity);\n}\n\n// Generic result wrapper — avoids exception-driven flow\npublic class Result&lt;T&gt;\n{\n    public bool IsSuccess { get; private init; }\n    public T? Value { get; private init; }\n    public string? Error { get; private init; }\n\n    public static Result&lt;T&gt; Success(T value) =&gt;\n        new() { IsSuccess = true, Value = value };\n\n    public static Result&lt;T&gt; Failure(string error) =&gt;\n        new() { IsSuccess = false, Error = error };\n}\n\n// Generic constraints\npublic class Repository&lt;T&gt; where T : class, IEntity, new() { ... }</code></pre><p>Generic constraints: <code>where T : class</code> (reference type), <code>where T : struct</code> (value type), <code>where T : new()</code> (parameterless constructor), <code>where T : IEntity</code> (must implement interface).</p>",
      "followUps": [
        {
          "question": "What is a generic constraint?",
          "answer": "<p>Generic constraints restrict what types can be used as the type argument for a generic class or method. <code>where T : class</code> — T must be a reference type. <code>where T : struct</code> — T must be a value type (enables <code>Nullable&lt;T&gt;</code>). <code>where T : new()</code> — T must have a parameterless constructor (allows <code>new T()</code> inside the generic method). <code>where T : IEntity</code> — T must implement the interface (enables calling interface members on T). <code>where T : BaseClass</code> — T must inherit from BaseClass. Multiple constraints: <code>where T : class, IEntity, new()</code>. Constraints allow the generic code to use type-specific members — without <code>where T : IEntity</code> you can't call <code>entity.Id</code> on T even if you know it has that property.</p>"
        },
        {
          "question": "What is the difference between covariance and contravariance?",
          "answer": "<p><strong>Covariance</strong> (<code>out T</code>) — allows a more derived type to be used where a less derived type is expected. <code>IEnumerable&lt;Dog&gt;</code> can be assigned to <code>IEnumerable&lt;Animal&gt;</code> because <code>IEnumerable&lt;out T&gt;</code> is covariant — you can only read from it, so a <code>Dog</code> sequence is safely usable as an <code>Animal</code> sequence. <strong>Contravariance</strong> (<code>in T</code>) — allows a less derived type to be used where a more derived type is expected. <code>Action&lt;Animal&gt;</code> can be assigned to <code>Action&lt;Dog&gt;</code> — an action that handles any Animal can handle Dogs. Covariance: producing values (returns). Contravariance: consuming values (parameters). In practice: most developers just need to know that generic collections are covariant for read-only interfaces (<code>IEnumerable&lt;out T&gt;</code>, <code>IReadOnlyList&lt;out T&gt;</code>) and contravariant for consumers (<code>IComparer&lt;in T&gt;</code>).</p>"
        }
      ],
      "tip": "<strong>Covariance (out T)</strong> — can use a more derived type. <code>IEnumerable&lt;Dog&gt;</code> can be assigned to <code>IEnumerable&lt;Animal&gt;</code>. <strong>Contravariance (in T)</strong> — can use a less derived type. Used on method parameters. Most developers just need to know generic collections are covariant when the type parameter is <code>out</code>.",
      "tags": ["oops", "generics", "csharp"],
      "sortOrder": 6,
      "isActive": true
    },
    {
      "id": "oops-007",
      "category": "oops",
      "difficulty": "medium",
      "frequency": "medium",
      "question": "What are delegates and events in C#?",
      "answer": "<p>A <strong>delegate</strong> is a type-safe function pointer — a reference to a method that matches a specific signature. <code>Action</code>, <code>Func&lt;T&gt;</code>, and <code>Predicate&lt;T&gt;</code> are built-in delegates.</p><pre><code class=\"language-csharp\">// Built-in delegates\nFunc&lt;int, bool&gt; isEven = x =&gt; x % 2 == 0;\nAction&lt;string&gt; log = Console.WriteLine;\n\n// LINQ uses delegates extensively\nvar activeItems = items.Where(p =&gt; p.IsActive);  // Func&lt;T, bool&gt;\n\n// Event (restricts direct invocation — only class can raise it)\npublic class PaymentProcessor\n{\n    public event EventHandler&lt;PaymentCompletedEventArgs&gt;? PaymentCompleted;\n\n    protected virtual void OnPaymentCompleted(PaymentCompletedEventArgs e)\n        =&gt; PaymentCompleted?.Invoke(this, e);\n}\n\n// Subscriber\npaymentProcessor.PaymentCompleted += async (sender, e) =&gt;\n    await SendReceiptEmailAsync(e.Invoice);</code></pre><p>An <strong>event</strong> wraps a delegate with add/remove accessors — only the declaring class can raise it. Implements the Observer pattern.</p>",
      "followUps": [
        {
          "question": "What is the difference between event and delegate?",
          "answer": "<p>A <strong>delegate field</strong> is a raw function pointer — anyone with access can assign to it (<code>_myDelegate = someMethod</code>) or invoke it directly (<code>_myDelegate()</code>). Exposing a public delegate field means external code can wipe out all existing subscribers by assigning (<code>=</code>) instead of adding (<code>+=</code>), or can invoke it directly. An <strong>event</strong> wraps a delegate with special add/remove accessors — external code can only <code>+=</code> (subscribe) or <code>-=</code> (unsubscribe). Only the declaring class can assign (<code>=</code>) or invoke (<code>Invoke()</code>) the event. This is the Observer pattern's encapsulation: the subject controls when the event fires; observers can only register/unregister their callbacks. Always use <code>event</code> for the Observer pattern — never expose a raw delegate field publicly.</p>"
        },
        {
          "question": "What is EventHandler<T>?",
          "answer": "<p><code>EventHandler&lt;TEventArgs&gt;</code> is the standard delegate type for events in .NET: <code>void Handler(object? sender, TEventArgs e)</code>. The <code>sender</code> is the object raising the event; <code>e</code> is an object derived from <code>EventArgs</code> containing event data. Using this standard signature means your events integrate seamlessly with .NET patterns and third-party libraries. Create event args by inheriting <code>EventArgs</code>: <code>public class PaymentCompletedEventArgs : EventArgs { public Invoice Invoice { get; init; } }</code>. For events with no data, use the non-generic <code>EventHandler</code> which uses plain <code>EventArgs</code>. The pattern: declare the event (<code>public event EventHandler&lt;MyArgs&gt;? MyEvent</code>), define a protected virtual <code>OnMyEvent</code> raiser method, call it from within the class, and null-check before invoking (<code>MyEvent?.Invoke(this, args)</code>).</p>"
        },
        {
          "question": "What are lambda expressions?",
          "answer": "<p>Lambda expressions are anonymous functions — inline functions defined with the <code>=&gt;</code> syntax. <code>x =&gt; x * 2</code> is a lambda that takes <code>x</code> and returns <code>x * 2</code>. They can be assigned to delegate types: <code>Func&lt;int, int&gt; double = x =&gt; x * 2</code>. Most commonly used in LINQ: <code>items.Where(x =&gt; x.IsActive).Select(x =&gt; x.Name)</code>. Lambdas can be <em>closures</em> — they capture variables from the enclosing scope: <code>var threshold = 100; items.Where(x =&gt; x.Amount &gt; threshold)</code> captures <code>threshold</code>. Expression lambdas (<code>x =&gt; x.Name</code>) are used by EF Core for query translation to SQL. Statement lambdas (<code>(x, y) =&gt; { var sum = x + y; return sum; }</code>) allow multi-statement bodies.</p>"
        }
      ],
      "tip": "<strong>Events vs delegates:</strong> A public delegate field lets anyone invoke it. An event wraps the delegate and restricts external code to += and -= only — the raising is encapsulated inside the class. Always use events for the observer pattern.",
      "tags": ["oops", "delegates", "events", "csharp"],
      "sortOrder": 7,
      "isActive": true
    },
    {
      "id": "oops-008",
      "category": "oops",
      "difficulty": "hard",
      "frequency": "medium",
      "question": "What is the difference between value types and reference types in C#?",
      "answer": "<p><strong>Value types</strong> — stored directly where declared (stack for locals). Copying creates an independent copy. Includes: <code>int</code>, <code>double</code>, <code>bool</code>, <code>DateTime</code>, <code>struct</code>, <code>enum</code>.</p><p><strong>Reference types</strong> — variable holds a reference (pointer) to heap-allocated data. Copying copies the reference, not the data. Includes: <code>class</code>, <code>string</code>, <code>array</code>, <code>interface</code>.</p><pre><code class=\"language-csharp\">// Value type: copy semantics\nDateTime d1 = DateTime.Now;\nDateTime d2 = d1;  // independent copy\nd2 = d2.AddDays(1);  // d1 unchanged\n\n// Reference type: shared reference\nvar list1 = new List&lt;int&gt; { 1, 2, 3 };\nvar list2 = list1;  // both point to same list!\nlist2.Add(4);  // list1 also has 4!\n\n// String is reference type but behaves like value type (immutable)\nstring s1 = \"hello\";\nstring s2 = s1;\ns2 += \" world\";  // creates a new string, s1 unchanged\n\n// Boxing: value type → object (heap allocation)\nobject boxed = 42;        // boxing\nint unboxed = (int)boxed; // unboxing — expensive, avoid in hot paths</code></pre>",
      "followUps": [
        {
          "question": "What is a nullable value type?",
          "answer": "<p>Value types cannot be <code>null</code> by default — <code>int</code> always has a value. <strong>Nullable value types</strong> (<code>int?</code>, <code>DateTime?</code>) wrap the value type in a <code>Nullable&lt;T&gt;</code> struct that adds a boolean <code>HasValue</code> flag. If <code>HasValue</code> is false, <code>Value</code> throws; use <code>GetValueOrDefault()</code> safely. Common uses: representing optional database columns (NULL), optional method parameters, representing \"not yet set\" state. C# 8 nullable reference types (<code>#nullable enable</code>) extend nullability annotations to reference types — the compiler warns when you dereference a potentially null reference. Both features have the same goal: making nullability explicit in the type system so null-related bugs are caught at compile time.</p>"
        },
        {
          "question": "What are C# records?",
          "answer": "<p><strong>Records</strong> (C# 9+) are reference types (or value types for <code>record struct</code>) with value semantics — two records are equal if all their properties are equal, not if they're the same instance. The compiler generates: <code>Equals()</code>, <code>GetHashCode()</code>, <code>ToString()</code>, and a copy constructor. Positional records: <code>public record Point(int X, int Y);</code> — properties are init-only, enabling non-destructive mutation via <code>with</code>: <code>var p2 = p1 with { X = 10 };</code>. Ideal for: DTOs, value objects, immutable domain primitives, CQRS commands/queries. In FolioTrack I use records for all request/response DTOs: <code>public record CreatePortfolioRequest(string Name, string? Description);</code> — concise, immutable, and equality works correctly for testing.</p>"
        },
        {
          "question": "What is struct vs class performance trade-off?",
          "answer": "<p><strong>Structs</strong> (value types) avoid heap allocation — they live on the stack (or inline in the containing object). Benefits: no GC pressure, better cache locality for small collections of structs. Cost: copying on assignment — passing a large struct to a method copies every field. Rule of thumb: structs are beneficial when they're small (16 bytes or less), immutable, and frequently allocated. Examples: <code>DateTime</code>, <code>Point</code>, <code>Guid</code>, <code>Vector3</code>. For large or mutable data, class is better — the heap allocation cost is small compared to the copy cost of a large struct. Use <code>ref struct</code> (like <code>Span&lt;T&gt;</code>) for stack-only high-performance scenarios. For most application-level types (services, domain entities, DTOs), always use <code>class</code>.</p>"
        }
      ],
      "tip": "<strong>Structs are copied by value</strong> — passing a large struct to a method copies every field. Use <code>in</code> parameter modifier to pass readonly reference: <code>void Process(in LargeStruct s)</code>. Or just use class if it's large or mutable.",
      "tags": ["oops", "value-types", "reference-types", "csharp", "memory"],
      "sortOrder": 8,
      "isActive": true
    },
    {
      "id": "oops-009",
      "category": "oops",
      "difficulty": "medium",
      "frequency": "very-high",
      "question": "What is CLR and how does .NET code execute?",
      "answer": "<p><strong>CLR (Common Language Runtime)</strong> is the virtual machine that executes .NET code. It provides: memory management (GC), type safety, exception handling, thread management, and security. When you write C#, the compiler produces <strong>IL (Intermediate Language)</strong> — not native machine code. The CLR then converts IL to native code at runtime via the <strong>JIT (Just-In-Time) compiler</strong>.</p><p><strong>Execution flow:</strong></p><ol><li>You write C# → C# compiler produces IL (in .dll/.exe)</li><li>Runtime: CLR's JIT compiler converts IL to native machine code on first execution of a method</li><li>Native code is cached in memory — subsequent calls run native code directly (no re-JIT)</li><li>GC automatically manages object lifetimes</li></ol><p><strong>Key CLR services:</strong></p><ul><li><strong>Garbage Collector</strong> — automatic memory management, generational (Gen0/Gen1/Gen2/LOH)</li><li><strong>Type safety verifier</strong> — prevents illegal memory access</li><li><strong>Exception handling</strong> — structured try/catch/finally across all .NET languages</li><li><strong>Thread support</strong> — ThreadPool, Tasks, async/await plumbing</li></ul><p>In .NET 8, the <strong>ReadyToRun (R2R)</strong> compilation model pre-compiles IL to native code at publish time, reducing JIT startup overhead — important for containerized microservices with cold starts.</p>",
      "followUps": [
        {
          "question": "What is Native AOT in .NET 8?",
          "answer": "<p><strong>Native AOT (Ahead-of-Time compilation)</strong> compiles the entire .NET application to self-contained native machine code at build time — no JIT, no CLR runtime needed. Benefits: startup time drops from ~100ms to single-digit milliseconds, memory footprint shrinks by 50-80%, container images are smaller. Ideal for: serverless functions, CLI tools, microservices with aggressive cold-start requirements. Trade-offs: no runtime code generation (Reflection.Emit, dynamic proxy generation), limited reflection support (must annotate with <code>[DynamicallyAccessedMembers]</code>), some libraries (EF Core, Newtonsoft.Json) aren't fully AOT-compatible (use System.Text.Json instead). Publish with <code>dotnet publish -r linux-x64 -p:PublishAot=true</code>.</p>"
        },
        {
          "question": "What is Roslyn?",
          "answer": "<p><strong>Roslyn</strong> is the open-source C# and VB.NET compiler platform. Unlike classic compilers (black-box executables), Roslyn exposes the compilation pipeline as APIs — you can parse C# source into a syntax tree, perform semantic analysis, and generate code, all from within a .NET application. This powers: IDE features (IntelliSense, refactoring, code analysis), Source Generators (Roslyn v4+), Analyzers (custom code quality rules), and scripting (<code>CSharpScript.EvaluateAsync()</code>). Source Generators run during compilation and emit additional C# source files — used by EF Core (compiled models), System.Text.Json (source-gen serializers), and regex (<code>[GeneratedRegex]</code>) to eliminate reflection at runtime.</p>"
        },
        {
          "question": "What is the difference between .NET CLR and Java JVM?",
          "answer": "<p>Both are managed runtime environments with JIT compilation and garbage collection, but differ in: <strong>Language support</strong> — CLR supports C#, F#, VB.NET (all compile to the same IL). JVM supports Java, Kotlin, Scala, Groovy. <strong>Value types</strong> — CLR has true value types (structs) that live on the stack without heap allocation. JVM treats everything as objects (boxing required for primitives in generics until Project Valhalla). <strong>Platform</strong> — CLR runs on Windows, Linux, macOS. JVM runs everywhere with JRE. <strong>AOT</strong> — CLR supports Native AOT (full ahead-of-time). JVM has GraalVM Native Image. <strong>Interop</strong> — CLR has P/Invoke for native code. JVM uses JNI. Both have evolved similarly; performance differences in practice are minimal for typical web applications.</p>"
        }
      ],
      "tip": "<strong>Native AOT (.NET 7+)</strong> compiles the entire application to native machine code at build time — no JIT at runtime. Startup time drops from ~100ms to single-digit ms, memory footprint shrinks. Trade-off: no runtime code generation (no Reflection.Emit), larger binary.",
      "tags": ["oops", "clr", "jit", "dotnet", "runtime"],
      "sortOrder": 9,
      "isActive": true
    },
    {
      "id": "oops-010",
      "category": "oops",
      "difficulty": "hard",
      "frequency": "high",
      "question": "How does Garbage Collection work in .NET? What are generations?",
      "answer": "<p><strong>Garbage Collection (GC)</strong> is the CLR's automatic memory manager. It reclaims memory occupied by unreachable objects (objects with no live references) so you don't have to manually free memory.</p><p><strong>Generational GC — the key concept:</strong></p><ul><li><strong>Gen0</strong> — short-lived objects (local variables, temporary allocations). Collected most frequently. Fast — typically takes &lt;1ms.</li><li><strong>Gen1</strong> — objects that survived Gen0 collection. Buffer between Gen0 and Gen2.</li><li><strong>Gen2</strong> — long-lived objects (static fields, large caches, DbContext). Collected rarely. Expensive — can take 100ms+.</li><li><strong>LOH (Large Object Heap)</strong> — objects &gt;85KB allocated directly here. Only collected during Gen2 (full GC).</li></ul><pre><code class=\"language-csharp\">// Prefer object pooling for frequently allocated large objects\nvar pool = new ObjectPool&lt;LargeBuffer&gt;(() =&gt; new LargeBuffer());\nvar buffer = pool.Get();\ntry {\n    // use buffer\n} finally {\n    pool.Return(buffer); // reuse instead of GC\n}\n\n// Dispose patterns trigger GC-eligible state\npublic class DatabaseContext : IDisposable\n{\n    private bool _disposed;\n\n    public void Dispose()\n    {\n        if (!_disposed) {\n            _connection?.Dispose();\n            _disposed = true;\n        }\n        GC.SuppressFinalize(this);  // don't need finalizer now\n    }\n}</code></pre><p>In FolioTrack, I kept <code>FundHoldingsService</code> as a Singleton and cached fund data in-memory (Gen2) — intentional, infrequently updated. Short-lived DTO projections stay in Gen0 and are collected quickly.</p>",
      "followUps": [
        {
          "question": "What is IDisposable and when should you implement it?",
          "answer": "<p>Implement <code>IDisposable</code> when your class holds <em>unmanaged resources</em> (file handles, database connections, network sockets, native memory) or <em>managed resources that are themselves disposable</em> (<code>SqlConnection</code>, <code>Stream</code>, <code>HttpClient</code>). The GC collects managed objects automatically, but it doesn't know how to release file handles or connections — those must be explicitly freed. The <code>using</code> statement guarantees <code>Dispose()</code> is called even if an exception occurs. The standard pattern: override <code>Dispose(bool disposing)</code>, call <code>GC.SuppressFinalize(this)</code> (skip the finalizer since resources are already freed), and set a <code>_disposed</code> flag to guard against double disposal. In ASP.NET Core DI, Scoped services are automatically disposed at the end of the request, but manually constructed objects need explicit <code>using</code>.</p>"
        },
        {
          "question": "What is a finalizer and when is it called?",
          "answer": "<p>A <strong>finalizer</strong> (destructor) is a method (<code>~MyClass()</code>) called by the GC before reclaiming an object's memory — it's the last resort for cleanup. Problems: the GC calls the finalizer on a background thread at an unpredictable time, potentially long after the object is last used. Objects with finalizers survive an extra GC cycle (promoted to Gen1 for finalization). They cannot be called directly and cannot safely access managed objects (which may already be collected). <code>GC.SuppressFinalize(this)</code> in <code>Dispose()</code> tells the GC to skip the finalizer since cleanup already happened. In practice, almost never write a finalizer — implement <code>IDisposable</code> instead. Finalizers are only for wrapping unmanaged resources via <code>SafeHandle</code> as a safety net if <code>Dispose()</code> wasn't called.</p>"
        },
        {
          "question": "How do you diagnose a memory leak in .NET?",
          "answer": "<p>Tools and approach: (1) <strong>dotnet-counters</strong> — monitor GC metrics in real time: <code>dotnet-counters monitor --process-id PID</code>. Watch for growing Gen2 collections and increasing working set. (2) <strong>dotnet-dump</strong> — capture a memory dump: <code>dotnet-dump collect -p PID</code>, analyze with <code>dotnet-dump analyze</code>. Use <code>dumpheap -stat</code> to find which types consume the most memory. (3) <strong>Application Insights / Azure Monitor</strong> — track memory metrics over time in production. (4) <strong>JetBrains dotMemory or Visual Studio Diagnostic Tools</strong> — take heap snapshots, compare them, find objects that grew. Common leak causes in .NET: event subscriptions not unsubscribed (<code>+=</code> without <code>-=</code>), static collections that grow forever, singleton services caching data unboundedly, captured closures holding large objects.</p>"
        }
      ],
      "tip": "<strong>GC pressure rule of thumb:</strong> Don't fight the GC — it's well-optimized for short-lived objects. Optimize when profiling shows Gen2 collections happening frequently (full GC). Use <code>dotnet-counters</code> or Application Insights to monitor GC stats in production.",
      "tags": ["oops", "gc", "garbage-collection", "memory", "dotnet"],
      "sortOrder": 10,
      "isActive": true
    },
    {
      "id": "oops-011",
      "category": "oops",
      "difficulty": "easy",
      "frequency": "high",
      "question": "What is the difference between var and dynamic in C#?",
      "answer": "<p>Both allow you to skip explicit type declarations, but they work completely differently:</p><table style=\"width:100%;font-size:.85rem;border-collapse:collapse;\"><tr><th>Feature</th><th>var</th><th>dynamic</th></tr><tr><td>Type resolution</td><td>Compile time (inferred from RHS)</td><td>Runtime (late binding)</td></tr><tr><td>Type safety</td><td>Fully type-safe</td><td>No compile-time checking</td></tr><tr><td>IntelliSense</td><td>Full IntelliSense</td><td>No IntelliSense</td></tr><tr><td>Performance</td><td>Same as explicit type</td><td>Slower (DLR overhead)</td></tr></table><pre><code class=\"language-csharp\">// var: compiler infers type at compile time\nvar name = \"Alice\";     // string — fully type-safe\nvar count = items.Where(x =&gt; x.Active).Count();  // int\nname = 42;              // COMPILER ERROR: can't assign int to string\n\n// dynamic: resolved at runtime — dangerous\ndynamic obj = \"hello\";\nobj = 42;               // no error — type changes at runtime!\nobj.NonExistentMethod(); // runtime exception: RuntimeBinderException\n\n// Valid use of dynamic: COM interop, reflection simplification\ndynamic excel = Activator.CreateInstance(Type.GetTypeFromProgID(\"Excel.Application\"));\nexcel.Visible = true;  // no need to cast</code></pre><p>I use <code>var</code> everywhere for readability. <code>dynamic</code> is rare — only for COM interop or deserializing unknown JSON shapes. In all my projects (FolioTrack, ServiceSelection) I avoid <code>dynamic</code>.</p>",
      "followUps": [
        {
          "question": "Can you use var for method parameters?",
          "answer": "<p>No — <code>var</code> cannot be used for method parameters, return types, or field declarations. It's only valid for local variable declarations where the right-hand side value is present and the type can be inferred: <code>var x = 5;</code>. The limitation exists because <code>var</code> requires an initializer expression to infer the type from — in a method parameter there's no initializer. Also, method signatures are part of the public API: making them explicit (<code>int id</code> not <code>var id</code>) makes intent clear to callers. C# 9 introduced a related feature for patterns: <code>if (obj is var x)</code> — here <code>var</code> in a pattern does work because the matched value provides the type.</p>"
        },
        {
          "question": "What is the DLR (Dynamic Language Runtime)?",
          "answer": "<p>The <strong>DLR (Dynamic Language Runtime)</strong> is a runtime environment built on top of the CLR that adds services for dynamic languages — late binding, dynamic dispatch, and interoperability between dynamic and static languages. It powers the <code>dynamic</code> keyword in C#, IronPython, and IronRuby. When you call a method on a <code>dynamic</code> object, the DLR resolves the call at runtime using a binder — it checks the actual type and dispatches to the correct method. The DLR uses caching (call site caching) to avoid re-resolving the same call repeatedly. Performance is significantly worse than static dispatch but much better than pure reflection. In practice, <code>dynamic</code> is only used for COM interop (where Excel, Word use late-bound dispatch) and occasionally for JSON-like loosely typed structures.</p>"
        }
      ],
      "tip": "<strong>var is NOT the same as dynamic.</strong> <code>var</code> is just syntactic sugar — the actual type is known at compile time. <code>dynamic</code> bypasses compile-time type checking entirely. Always prefer <code>var</code> over <code>dynamic</code> unless you specifically need late binding.",
      "tags": ["oops", "csharp", "var", "dynamic", "type-system"],
      "sortOrder": 11,
      "isActive": true
    },
    {
      "id": "oops-012",
      "category": "oops",
      "difficulty": "medium",
      "frequency": "high",
      "question": "What is the difference between const, readonly, and static readonly?",
      "answer": "<p>All three create fields that can't be arbitrarily changed, but their timing and scope differ:</p><table style=\"width:100%;font-size:.85rem;border-collapse:collapse;\"><tr><th>Feature</th><th>const</th><th>readonly</th><th>static readonly</th></tr><tr><td>When set</td><td>Compile time</td><td>Constructor or initializer</td><td>Static constructor or initializer</td></tr><tr><td>Can change per instance</td><td>No</td><td>Yes (in constructor)</td><td>No (single value)</td></tr><tr><td>Runtime value allowed</td><td>No (literal only)</td><td>Yes</td><td>Yes</td></tr></table><pre><code class=\"language-csharp\">public class Config\n{\n    // const: baked into calling assembly at compile time\n    public const int MaxRetries = 3;  // must be a literal\n    public const string DefaultCulture = \"en-US\";\n\n    // readonly: set once in constructor, can vary per instance\n    private readonly string _connectionString;\n    public Config(string cs) { _connectionString = cs; }\n\n    // static readonly: set once at class initialization, runtime value OK\n    public static readonly DateTime AppStartTime = DateTime.UtcNow;\n    public static readonly string Version =\n        Assembly.GetExecutingAssembly().GetName().Version!.ToString();\n}</code></pre><p><strong>Key gotcha with const:</strong> If you change a <code>const</code> value in assembly A, assembly B must be recompiled to see the new value — the old value was baked in at compile time. Use <code>static readonly</code> if the value might change between versions.</p>",
      "followUps": [
        {
          "question": "What is a readonly struct?",
          "answer": "<p>A <code>readonly struct</code> guarantees that none of its members mutate the struct's state — the compiler enforces it. All fields must be <code>readonly</code> or <code>init</code>-only. Benefit: the compiler can pass <code>readonly struct</code> by reference automatically (avoiding copies) without risk of mutation. When you call a method on a non-readonly struct through a <code>readonly</code> variable, the compiler makes a defensive copy to prevent mutation — this copy is invisible and can cause subtle bugs. Declaring it <code>readonly struct</code> eliminates defensive copies. <code>System.DateTime</code>, <code>System.Guid</code>, and <code>System.TimeSpan</code> are all <code>readonly struct</code>. Use <code>readonly struct</code> for small value objects that are truly immutable.</p>"
        },
        {
          "question": "When would you use const vs static readonly?",
          "answer": "<p>Use <strong><code>const</code></strong> for values that are truly universal constants and will never change across library versions: mathematical constants (<code>Math.PI</code>), application-defined magic numbers that are never going to be different, or string literals used in switch/pattern matching. Use <strong><code>static readonly</code></strong> for values that: are computed at runtime (can't use <code>const</code>), might change between library versions (to avoid recompile-all problems), are complex types (arrays, objects — <code>const</code> only allows primitives and strings), or need to be lazy-initialized. A common mistake: <code>public const string ApiVersion = \"v1\"</code> in a NuGet package — if you release <code>\"v2\"</code>, all consumers must recompile to pick it up. Use <code>static readonly</code> instead.</p>"
        }
      ],
      "tip": "<strong>Use static readonly instead of const for public API values</strong> that might change between library versions. const is baked into the calling assembly — a version bump without recompile will use the old value. static readonly is evaluated fresh at runtime.",
      "tags": ["oops", "const", "readonly", "csharp"],
      "sortOrder": 12,
      "isActive": true
    },
    {
      "id": "oops-013",
      "category": "oops",
      "difficulty": "medium",
      "frequency": "high",
      "question": "What is the IDisposable pattern and when do you implement it?",
      "answer": "<p><strong>IDisposable</strong> is a contract for releasing unmanaged resources (file handles, database connections, network sockets) deterministically — without waiting for the GC. Implement it when your class holds resources that must be freed promptly.</p><pre><code class=\"language-csharp\">public class ReportGenerator : IDisposable\n{\n    private Stream? _outputStream;\n    private bool _disposed;\n\n    public ReportGenerator(string filePath)\n    {\n        _outputStream = File.Create(filePath);\n    }\n\n    public void GenerateReport() { /* write to stream */ }\n\n    public void Dispose()\n    {\n        Dispose(disposing: true);\n        GC.SuppressFinalize(this); // finalizer not needed\n    }\n\n    protected virtual void Dispose(bool disposing)\n    {\n        if (!_disposed)\n        {\n            if (disposing)\n                _outputStream?.Dispose();\n            _disposed = true;\n        }\n    }\n}\n\n// Usage — C# using statement guarantees Dispose() is called\nusing var generator = new ReportGenerator(\"report.pdf\");\ngenerator.GenerateReport();\n// Dispose() called automatically at end of using block\n\n// C# 8+ using declaration (same behavior, less nesting)\nusing var conn = new SqlConnection(connectionString);</code></pre><p>The <code>using</code> statement is syntactic sugar for a try/finally block that calls <code>Dispose()</code>. In all my projects, any class inheriting from EF Core's DbContext, SqlConnection, or HttpClient flows through <code>using</code> blocks.</p>",
      "followUps": [
        {
          "question": "What is the difference between Dispose and Finalize?",
          "answer": "<p><strong>Dispose</strong> is deterministic — called explicitly by the developer via <code>using</code> or direct call. Releases resources immediately when the code is done with the object. Runs on the calling thread. <strong>Finalize</strong> (finalizer/destructor) is non-deterministic — called by the GC at an unknown time, potentially long after the object is last used. Runs on a dedicated GC finalizer thread. Should only be used as a safety net when <code>Dispose()</code> was not called. The standard pattern implements both: <code>Dispose()</code> releases resources and calls <code>GC.SuppressFinalize(this)</code> (skip the finalizer); the finalizer calls <code>Dispose(false)</code> as a backstop. If you implement <code>IDisposable</code> properly, the finalizer should never run in correctly written code.</p>"
        },
        {
          "question": "What does GC.SuppressFinalize do?",
          "answer": "<p><code>GC.SuppressFinalize(this)</code> tells the GC: \"Don't call the finalizer on this object.\" When an object has a finalizer, the GC places it in the finalization queue before collecting it — this extends its lifetime by one GC cycle. If you've already released resources in <code>Dispose()</code>, the finalizer is unnecessary. Calling <code>GC.SuppressFinalize(this)</code> in <code>Dispose()</code> removes the object from the finalization queue — it can be collected immediately in the next GC cycle without the extra overhead of finalization. Always call it in <code>Dispose()</code> if your class has a finalizer, as an optimization to avoid the unnecessary delay and thread context switch for finalization.</p>"
        },
        {
          "question": "What is IAsyncDisposable?",
          "answer": "<p><code>IAsyncDisposable</code> (C# 8+) allows async cleanup — when releasing resources requires awaiting async operations (flushing a buffer to a network stream, closing a database connection with an async call). Implement <code>ValueTask DisposeAsync()</code>. Use with <code>await using</code>: <code>await using var conn = new AsyncDbConnection();</code>. EF Core's <code>DbContext</code> implements both <code>IDisposable</code> and <code>IAsyncDisposable</code> — prefer <code>await using</code> in async code. If a class implements both, always call <code>DisposeAsync()</code> in async contexts — using the sync <code>Dispose()</code> in async code may block, which can cause deadlocks in certain synchronization contexts.</p>"
        }
      ],
      "tip": "<strong>In ASP.NET Core DI, registered services are automatically disposed</strong> at the end of the request (Scoped) or app shutdown (Singleton). You still need <code>using</code> for objects created manually (new SqlConnection, new StreamReader, etc.).",
      "tags": ["oops", "idisposable", "memory", "using", "csharp"],
      "sortOrder": 13,
      "isActive": true
    },
    {
      "id": "oops-014",
      "category": "oops",
      "difficulty": "easy",
      "frequency": "high",
      "question": "What is the difference between string and StringBuilder in C#?",
      "answer": "<p><strong>string</strong> is immutable — every concatenation or modification creates a new string object on the heap. In a loop, this generates enormous GC pressure.</p><p><strong>StringBuilder</strong> is mutable — it maintains an internal buffer and appends without creating new objects each time.</p><pre><code class=\"language-csharp\">// BAD: creates 1000 string objects\nstring result = \"\";\nfor (int i = 0; i &lt; 1000; i++)\n    result += i.ToString();  // each += allocates a new string!\n\n// GOOD: one StringBuilder, minimal allocations\nvar sb = new StringBuilder(capacity: 5000); // pre-size for efficiency\nfor (int i = 0; i &lt; 1000; i++)\n    sb.Append(i);\nstring result = sb.ToString();  // single string created at end\n\n// Real-world example: email body builder in PaymentReminder service\nvar emailBody = new StringBuilder();\nemailBody.AppendLine(\"Dear \").Append(client.Name).AppendLine(\",\");\nemailBody.AppendLine($\"Your invoice #{inv.Id} of {inv.Amount:C} is due.\");\nemailBody.AppendLine(\"Regards, Billing Team\");\nstring body = emailBody.ToString();</code></pre><p><strong>When to use what:</strong></p><ul><li><strong>string</strong> — concatenating 2-3 strings, string literals, simple operations</li><li><strong>StringBuilder</strong> — concatenating in loops, building large strings, HTML/email body generation</li><li><strong>string.Join()</strong> — joining a known collection — fastest option, internally uses efficient buffer</li></ul>",
      "followUps": [
        {
          "question": "What is string interning?",
          "answer": "<p><strong>String interning</strong> is an optimization where the CLR maintains a pool of unique string literals — if two string literals have the same value, they share the same memory object. At compile time, all identical string literals in the assembly are interned automatically: <code>string a = \"hello\"; string b = \"hello\";</code> — both reference the same interned object, so <code>object.ReferenceEquals(a, b)</code> is <code>true</code>. You can manually intern at runtime: <code>string interned = string.Intern(dynamicString)</code>. Useful if you have many duplicate strings loaded from a database or file. <code>string.IsInterned(s)</code> returns the interned instance if it exists, or null. Don't over-use manual interning — interned strings are never GC'd, so interning many unique strings causes memory growth.</p>"
        },
        {
          "question": "What is Span<char> for string operations?",
          "answer": "<p><code>Span&lt;char&gt;</code> and <code>ReadOnlySpan&lt;char&gt;</code> allow zero-allocation string slicing — you get a view into an existing string without copying characters. Example: <code>ReadOnlySpan&lt;char&gt; slice = \"Hello World\".AsSpan(6, 5);</code> creates a span pointing to \"World\" without allocating a new string. APIs like <code>int.Parse(ReadOnlySpan&lt;char&gt;)</code> work directly on spans, avoiding the intermediate string allocation. Critical for high-performance parsers, log processors, or any code that needs to split or scan strings in hot paths. The trade-off: <code>Span&lt;T&gt;</code> is a <code>ref struct</code> — it cannot be stored on the heap, used in async methods, or stored in class fields. Use it for local processing; convert to <code>string</code> with <code>.ToString()</code> when you need to store or pass the result.</p>"
        }
      ],
      "tip": "<strong>string.Join is often faster than StringBuilder for collections:</strong> <code>string.Join(\", \", items)</code> is cleaner and optimized. Use StringBuilder when you're building a string step-by-step with conditional logic.",
      "tags": ["oops", "string", "stringbuilder", "csharp", "performance"],
      "sortOrder": 14,
      "isActive": true
    },
    {
      "id": "oops-015",
      "category": "oops",
      "difficulty": "easy",
      "frequency": "medium",
      "question": "What is a sealed class and when would you use it?",
      "answer": "<p>A <strong>sealed</strong> class cannot be inherited — it terminates the inheritance chain. A sealed method in an unsealed class prevents further overriding in derived classes.</p><pre><code class=\"language-csharp\">// Sealed class — no further inheritance allowed\npublic sealed class JwtHelper\n{\n    private readonly string _secret;\n    public JwtHelper(string secret) { _secret = secret; }\n\n    public string GenerateToken(int userId, string email)\n    {\n        // JWT generation logic\n    }\n}\n\n// Cannot extend: public class ExtendedJwtHelper : JwtHelper {} // COMPILE ERROR\n\n// Sealed method — prevent overriding in further derived classes\npublic class PaymentBase\n{\n    protected virtual void Validate() { ... }\n}\n\npublic class CreditCardPayment : PaymentBase\n{\n    public sealed override void Validate() { ... } // cannot be overridden further\n}</code></pre><p><strong>When to use sealed:</strong></p><ul><li>Classes designed as utilities with no extension points (<code>JwtHelper</code>, static-like classes)</li><li>Performance-critical classes — JIT can devirtualize calls on sealed classes (inlining opportunity)</li><li>Security — prevent substitution attacks via inheritance</li><li>Value types (structs) are implicitly sealed</li></ul>",
      "followUps": [
        {
          "question": "Does sealing a class improve performance?",
          "answer": "<p>Yes, marginally, in specific scenarios. When the JIT knows a class is sealed, it can <em>devirtualize</em> virtual method calls — instead of an indirect dispatch through the vtable, it can call the method directly (or inline it). This eliminates the vtable lookup overhead and enables further optimizations. The performance difference is measurable in microbenchmarks but negligible for most application code. Where it matters: high-frequency hot paths like collection iterators (that's why <code>List&lt;T&gt;.Enumerator</code> is a struct), parsers, and number-crunching loops. Don't seal classes just for performance — seal them when it makes semantic sense (utility classes, security constraints) and performance is a bonus.</p>"
        },
        {
          "question": "Why is string sealed in .NET?",
          "answer": "<p><code>string</code> is sealed primarily for <strong>security and correctness</strong>, not performance. If <code>string</code> were extensible, an attacker could create a subclass that overrides <code>GetHashCode()</code> to return unpredictable values — breaking all Dictionary and HashSet lookups that use string keys. They could override <code>Equals()</code> to make <code>\"admin\" == \"hacker\"</code> return true under specific conditions — enabling privilege escalation. The CLR's string interning (shared references to identical literals) would also break if a subclass could modify \"shared\" strings. Many framework types are sealed for the same reason: <code>Guid</code>, <code>DateTime</code>, and other value-semantic types where the identity contract must be unbreakable.</p>"
        }
      ],
      "tip": "<strong>String is sealed for safety:</strong> If string were extensible, an attacker could create a malicious subclass with a modified <code>GetHashCode()</code> or <code>Equals()</code> — breaking Dictionary lookups and security checks. Many framework types are sealed for the same reason.",
      "tags": ["oops", "sealed", "inheritance", "csharp"],
      "sortOrder": 15,
      "isActive": true
    },
    {
      "id": "oops-016",
      "category": "oops",
      "difficulty": "medium",
      "frequency": "high",
      "question": "What is exception handling in C#? What is the difference between throw and throw ex?",
      "answer": "<p>Exception handling in C# uses <code>try-catch-finally</code> blocks. The <code>try</code> block contains code that might throw. The <code>catch</code> block handles specific exception types. The <code>finally</code> block runs regardless of whether an exception occurred — used for cleanup.</p><pre><code class=\"language-csharp\">// try-catch-finally pattern\npublic async Task&lt;Invoice&gt; GenerateInvoiceAsync(int assignmentId)\n{\n    SqlConnection? conn = null;\n    try\n    {\n        conn = new SqlConnection(_connectionString);\n        await conn.OpenAsync();\n        // ... invoice generation\n        return invoice;\n    }\n    catch (SqlException ex) when (ex.Number == 1205)  // deadlock\n    {\n        _logger.LogWarning(\"Deadlock on invoice {Id}, retrying\", assignmentId);\n        throw;  // rethrow — preserve stack trace\n    }\n    catch (Exception ex)\n    {\n        _logger.LogError(ex, \"Invoice generation failed for {Id}\", assignmentId);\n        throw;  // CORRECT: throw preserves original stack trace\n        // throw ex; // WRONG: resets stack trace to this line\n    }\n    finally\n    {\n        conn?.Dispose(); // always runs\n    }\n}</code></pre><p><strong>throw vs throw ex:</strong></p><ul><li><code>throw</code> — rethrows the exception preserving the original stack trace (where it actually happened)</li><li><code>throw ex</code> — resets the stack trace to the current line — you lose the original call stack, making debugging much harder</li></ul><p><strong>Exception filters</strong> (<code>catch (Exception ex) when (condition)</code>) — the filter runs before the stack unwinds, preserving the full stack trace while allowing conditional handling.</p>",
      "followUps": [
        {
          "question": "What is AggregateException?",
          "answer": "<p><code>AggregateException</code> wraps one or more exceptions that occurred during parallel or async operations. It's thrown by <code>Task.Wait()</code>, <code>Task.Result</code>, and <code>Task.WhenAll()</code> when one or more tasks fail. The inner exceptions are in <code>AggregateException.InnerExceptions</code>. When awaiting with <code>await</code>, ASP.NET Core unwraps the first inner exception and throws it directly (not as AggregateException) — so <code>await Task.WhenAll(tasks)</code> followed by catching specific exceptions works naturally. Use <code>AggregateException.Handle(ex => { ... return true; })</code> to handle specific types and rethrow the rest. The <code>Flatten()</code> method collapses nested AggregateExceptions into one.</p>"
        },
        {
          "question": "What is custom exception creation?",
          "answer": "<p>Create custom exceptions by inheriting from <code>Exception</code> (or a more specific subclass). Include the three standard constructors: parameterless, message-only, and message+inner: <code>public class InvoiceNotFoundException : Exception { public InvoiceNotFoundException() : base() {} public InvoiceNotFoundException(string msg) : base(msg) {} public InvoiceNotFoundException(string msg, Exception inner) : base(msg, inner) {} }</code>. Add domain-specific properties: <code>public int InvoiceId { get; }</code>. Use <code>[Serializable]</code> if the exception needs to cross AppDomain boundaries (legacy/remoting). Custom exceptions make catch blocks precise — <code>catch (InvoiceNotFoundException)</code> vs <code>catch (Exception)</code> — and carry domain context. Avoid creating custom exceptions just for messaging; use them when the caller needs to handle the exception type differently from general exceptions.</p>"
        },
        {
          "question": "What is exception filter?",
          "answer": "<p>An <strong>exception filter</strong> is a <code>when</code> clause on a catch block: <code>catch (SqlException ex) when (ex.Number == 1205)</code>. The filter expression is evaluated <em>before</em> the stack unwinds — if the filter returns false, the exception continues propagating to the next catch block as if this one didn't exist. Key advantage over nested <code>if</code>: when the filter returns false, the stack trace is preserved in full (no partial unwind). This is crucial for debugging tools and logging that need to see the original throw site. Exception filters are also useful for: conditional retry logic (catch transient exceptions only), logging all exceptions without handling them (<code>when (Log(ex))</code> always returns false), and handling exceptions from a specific source (<code>when (ex.Source == \"MyLibrary\")</code>).</p>"
        }
      ],
      "tip": "<strong>Always use <code>throw</code> not <code>throw ex</code></strong> when rethrowing. The stack trace is your most valuable debugging tool in production. Losing it with <code>throw ex</code> forces you to reproduce the bug rather than reading the trace.",
      "tags": ["oops", "exceptions", "try-catch", "csharp"],
      "sortOrder": 16,
      "isActive": true
    },
    {
      "id": "oops-017",
      "category": "oops",
      "difficulty": "easy",
      "frequency": "high",
      "question": "What are lambda expressions and how are they used in C#?",
      "answer": "<p>A <strong>lambda expression</strong> is an anonymous function — a short inline function that can be assigned to a delegate or passed as a method argument. Syntax: <code>parameters =&gt; expression</code>.</p><pre><code class=\"language-csharp\">// Basic lambda\nFunc&lt;int, int&gt; square = x =&gt; x * x;\nConsole.WriteLine(square(5)); // 25\n\n// Multi-parameter\nFunc&lt;int, int, int&gt; add = (a, b) =&gt; a + b;\n\n// Statement lambda (block body)\nFunc&lt;int, bool&gt; isPrime = n =&gt; {\n    if (n &lt; 2) return false;\n    for (int i = 2; i &lt;= Math.Sqrt(n); i++)\n        if (n % i == 0) return false;\n    return true;\n};\n\n// In LINQ (most common usage)\nvar activePortfolios = portfolios\n    .Where(p =&gt; p.IsActive)                     // filter\n    .Select(p =&gt; new { p.Id, p.Name })           // project\n    .OrderByDescending(p =&gt; p.CreatedAt)          // sort\n    .ToList();\n\n// In event handlers\nbtnSave.Click += async (sender, e) =&gt; await SaveAsync();\n\n// Capturing variables (closure)\nstring prefix = \"Hello\";\nAction&lt;string&gt; greet = name =&gt; Console.WriteLine($\"{prefix}, {name}!\"); // captures prefix\ngreet(\"Alice\"); // \"Hello, Alice!\"\n\n// C# 9+ shorter lambda for property access\nvar names = people.Select(p =&gt; p.Name).ToList(); // common pattern</code></pre><p>Lambdas are the backbone of LINQ and make collection operations concise. I use them throughout FolioTrack's service layer for filtering and projecting EF Core queries.</p>",
      "followUps": [
        {
          "question": "What is the difference between Func and Action?",
          "answer": "<p><strong><code>Action</code></strong> is a delegate for a method that returns <code>void</code>: <code>Action</code> (no params), <code>Action&lt;T&gt;</code> (one param), <code>Action&lt;T1, T2&gt;</code> (two params), up to 16. <strong><code>Func</code></strong> is a delegate for a method that returns a value — the last type parameter is always the return type: <code>Func&lt;int&gt;</code> (no params, returns int), <code>Func&lt;string, int&gt;</code> (takes string, returns int), <code>Func&lt;T1, T2, TResult&gt;</code>. <strong><code>Predicate&lt;T&gt;</code></strong> is equivalent to <code>Func&lt;T, bool&gt;</code> — takes one argument, returns bool. Used in LINQ <code>Find()</code>. In practice: use <code>Action</code> for callbacks/event handlers with no return value; use <code>Func</code> for transformations and selectors; use <code>Predicate</code> only when an API specifically requires it.</p>"
        },
        {
          "question": "What is a closure and what are the capture gotchas?",
          "answer": "<p>A <strong>closure</strong> is a lambda that captures variables from the enclosing scope — the variable is shared between the lambda and the outer code. The lambda holds a reference to the variable, not a copy of its value at creation time. <strong>Classic gotcha with loops:</strong> <code>for (int i = 0; i &lt; 5; i++) { buttons[i].Click += (s, e) =&gt; Console.WriteLine(i); }</code> — all button handlers print <code>5</code> (the final value of <code>i</code> after the loop). Fix: capture a copy — <code>var captured = i; buttons[i].Click += (s, e) =&gt; Console.WriteLine(captured);</code>. Another gotcha: closures in async methods — if you capture a variable and the method awaits something, the variable's value at the time of execution may differ from when the lambda was created. Always be explicit about what you're capturing.</p>"
        }
      ],
      "tip": "<strong>Closure capture gotcha with loops:</strong> Capturing a loop variable in a lambda captures the variable, not its current value. By the time the lambda executes, the loop may have finished and the variable holds the last value. Fix: <code>var captured = loopVar; var lambda = () =&gt; Use(captured);</code>",
      "tags": ["oops", "lambda", "delegates", "linq", "csharp"],
      "sortOrder": 17,
      "isActive": true
    },
    {
      "id": "oops-018",
      "category": "oops",
      "difficulty": "easy",
      "frequency": "very-high",
      "question": "What are access modifiers in C#? Explain public, private, protected, internal, and protected internal.",
      "answer": "<p><strong>Access modifiers</strong> control which code can access a type or member. They are the primary tool for encapsulation in C#.</p><table><tr><th>Modifier</th><th>Accessible from</th><th>Common use</th></tr><tr><td><code>public</code></td><td>Any code in any assembly</td><td>Public API — controllers, service interfaces, DTOs</td></tr><tr><td><code>private</code></td><td>The same class only</td><td>Internal implementation details, backing fields</td></tr><tr><td><code>protected</code></td><td>Same class + derived classes</td><td>Base class members that subclasses need to override/extend</td></tr><tr><td><code>internal</code></td><td>Same assembly (.dll/.exe) only</td><td>Implementation classes not meant to be public API</td></tr><tr><td><code>protected internal</code></td><td>Same assembly OR derived classes</td><td>Rare — base class methods accessible to subclasses in other assemblies</td></tr><tr><td><code>private protected</code> (C# 7.2)</td><td>Same class + derived classes in same assembly</td><td>Stricter than protected — derived classes outside the assembly can't access it</td></tr></table><pre><code class=\"language-csharp\">public class UserProgress  // public: accessible from controllers, services\n{\n    private int _confidenceLevel;  // private: only this class can touch it\n\n    public int ConfidenceLevel    // public: the outside world reads via property\n    {\n        get =&gt; _confidenceLevel;\n        private set  // private set: only this class can write it\n        {\n            if (value &lt; 1 || value &gt; 5)\n                throw new ArgumentOutOfRangeException(nameof(value), \"1–5 only\");\n            _confidenceLevel = value;\n        }\n    }\n\n    protected virtual void OnProgressChanged() { }  // subclasses can override\n    internal void ResetForTesting() { _confidenceLevel = 0; } // test assembly only\n}</code></pre>",
      "followUps": [
        {
          "question": "What is the default access modifier for class members in C#?",
          "answer": "<p>For <strong>class members</strong> (fields, methods, properties): the default is <code>private</code>. If you declare a method without an access modifier inside a class, it's private. For <strong>top-level types</strong> (classes, interfaces, enums at namespace level): the default is <code>internal</code> — not accessible outside the assembly unless explicitly marked <code>public</code>. For <strong>members of an interface</strong>: the default is <code>public</code> (and abstract) — interface members are always public because interfaces define contracts that implementors must fulfill. For <strong>enum members</strong>: always public. For <strong>struct members</strong>: same as class — default private. In practice, always write access modifiers explicitly rather than relying on defaults. It makes code intent clear and prevents accidental exposure.</p>"
        },
        {
          "question": "What is the InternalsVisibleTo attribute?",
          "answer": "<p><code>[InternalsVisibleTo(\"AssemblyName\")]</code> is an assembly-level attribute that grants a specific external assembly access to <code>internal</code> members of the current assembly. The most common use case is unit testing: your production code in <code>MyApp.dll</code> marks internal classes as <code>internal</code> (not public), but the test project <code>MyApp.Tests.dll</code> needs to test them. Add <code>[assembly: InternalsVisibleTo(\"MyApp.Tests\")]</code> to the production assembly's <code>AssemblyInfo.cs</code> (or any .cs file). Now all internal types are visible to the test project. This is better than making everything public just for testing — you keep your API surface small while still being able to write focused unit tests on implementation details. For signed assemblies, you must also include the public key token of the test assembly.</p>"
        }
      ],
      "tip": "<strong>Interview tip:</strong> Default for class members = <code>private</code>. Default for top-level types = <code>internal</code>. The most restrictive access modifier that still works is always preferred — it minimises the public API surface and makes refactoring easier. If you can make it <code>private</code>, do it.",
      "tags": ["oops", "access-modifiers", "encapsulation", "csharp"],
      "sortOrder": 18,
      "isActive": true
    },
    {
      "id": "oops-019",
      "category": "oops",
      "difficulty": "easy",
      "frequency": "high",
      "question": "What is the difference between float, double, and decimal in C#? When do you use each?",
      "answer": "<p>All three are numeric types but differ critically in <strong>precision</strong>, <strong>range</strong>, and <strong>intended use</strong>.</p><table><tr><th>Type</th><th>Size</th><th>Precision</th><th>Range</th><th>Use</th></tr><tr><td><code>float</code></td><td>4 bytes</td><td>~7 significant digits</td><td>±3.4 × 10<sup>38</sup></td><td>Graphics, game physics (GPU uses float)</td></tr><tr><td><code>double</code></td><td>8 bytes</td><td>~15–16 significant digits</td><td>±1.7 × 10<sup>308</sup></td><td>Scientific calculations, general-purpose decimals</td></tr><tr><td><code>decimal</code></td><td>16 bytes</td><td>28–29 significant digits</td><td>±7.9 × 10<sup>28</sup></td><td><strong>Money, financial calculations</strong> — no rounding errors</td></tr></table><pre><code class=\"language-csharp\">// The critical difference — floating-point binary representation\ndouble d = 0.1 + 0.2;\nConsole.WriteLine(d);          // 0.30000000000000004 ← NOT 0.3!\n\ndecimal m = 0.1m + 0.2m;\nConsole.WriteLine(m);          // 0.3 ← exactly correct\n\n// In FolioTrack — financial data uses decimal throughout\npublic class Holding\n{\n    public decimal PurchasePrice { get; set; }   // ✅ decimal for money\n    public decimal CurrentNAV { get; set; }      // ✅ decimal for NAV\n    public decimal Quantity { get; set; }        // ✅ decimal for units\n    public decimal ReturnPercent =&gt;              // calculated, still decimal\n        ((CurrentNAV - PurchasePrice) / PurchasePrice) * 100;\n}\n\n// In SQL Server: map to DECIMAL(18,4) for money columns\n// In Entity Framework:\n// modelBuilder.Entity&lt;Holding&gt;()\n//     .Property(h =&gt; h.PurchasePrice)\n//     .HasColumnType(\"decimal(18,4)\");</code></pre>",
      "followUps": [
        {
          "question": "Why does 0.1 + 0.2 not equal 0.3 in double?",
          "answer": "<p>Computers store <code>float</code> and <code>double</code> in <strong>binary floating-point</strong> (IEEE 754 standard). Just as 1/3 cannot be represented exactly in decimal (0.333…), many decimal fractions cannot be represented exactly in binary. The value 0.1 in binary is 0.0001100110011… (repeating) — it gets rounded to the nearest representable binary value. When you add two slightly-off binary approximations, the error compounds. This is a fundamental property of binary floating-point, not a C# bug. <code>decimal</code> avoids this because it stores numbers in <strong>base-10</strong> (decimal floating-point), so 0.1, 0.2, and 0.3 are stored exactly. The trade-off: <code>decimal</code> is 4x larger (16 bytes vs 4 for float), slower (no CPU hardware decimal unit — it's software-emulated), and has a smaller range. Always use <code>decimal</code> for money; never use <code>float</code> or <code>double</code> for financial calculations.</p>"
        },
        {
          "question": "What suffix do you use for decimal and float literals?",
          "answer": "<p>Literal suffixes disambiguate numeric types: <code>1.5f</code> or <code>1.5F</code> = <code>float</code>. Without suffix, <code>1.5</code> = <code>double</code> (default for floating-point literals). <code>1.5m</code> or <code>1.5M</code> = <code>decimal</code>. Without the <code>m</code> suffix, assigning a literal to a decimal variable causes a compile error: <code>decimal price = 19.99;</code> → error CS0664. You must write <code>decimal price = 19.99m;</code>. For integers: <code>100L</code> = <code>long</code>, <code>100U</code> = <code>uint</code>, <code>100UL</code> = <code>ulong</code>. The suffix requirement for <code>decimal</code> is a common source of subtle bugs when refactoring code — always double check that financial calculations use the <code>m</code> suffix.</p>"
        }
      ],
      "tip": "<strong>Rule:</strong> Money and financial calculations → always <code>decimal</code>. Scientific/physics calculations → <code>double</code>. GPU/graphics → <code>float</code>. Never mix <code>float</code>/<code>double</code> with <code>decimal</code> without explicit cast. In SQL Server, <code>MONEY</code> and <code>DECIMAL(p,s)</code> columns should map to C# <code>decimal</code>.",
      "tags": ["oops", "csharp", "decimal", "double", "float", "numeric"],
      "sortOrder": 19,
      "isActive": true
    },
    {
      "id": "oops-020",
      "category": "oops",
      "difficulty": "medium",
      "frequency": "high",
      "question": "What is JIT (Just-In-Time) compilation and how does .NET code execute?",
      "answer": "<p>Understanding .NET's execution model is a common interview topic that connects C#, CLR, and performance.</p><p><strong>Two-stage compilation process:</strong></p><ol><li><strong>Stage 1 — C# → IL (Intermediate Language):</strong> When you build a .NET project, the C# compiler (<code>csc</code> / Roslyn) compiles C# source into <strong>IL (Intermediate Language)</strong>, also called MSIL or CIL. IL is CPU-architecture-neutral bytecode stored in <code>.dll</code> or <code>.exe</code> assemblies. This is what NuGet packages and your compiled output contain.</li><li><strong>Stage 2 — IL → Native code (JIT):</strong> When the app runs, the <strong>CLR (Common Language Runtime)</strong> reads IL and the <strong>JIT compiler</strong> compiles each method to native machine code <em>the first time it is called</em>. The native code is cached in memory so subsequent calls run at full native speed.</li></ol><pre><code class=\"language-text\">C# Source (.cs)\n     │\n     ▼  [Build time — Roslyn compiler]\nIL Bytecode (.dll assembly)\n     │\n     ▼  [Runtime — first method call]\nJIT Compiler (RyuJIT in .NET Core)\n     │\n     ▼\nNative Machine Code (x64 / ARM)\n     │\n     ▼\nCPU executes at full speed</code></pre><p><strong>RyuJIT</strong> is the current JIT compiler in .NET Core/.NET 5+. It performs aggressive optimisations: inlining, loop unrolling, dead-code elimination, vectorisation (SIMD).</p>",
      "followUps": [
        {
          "question": "What is AOT (Ahead-Of-Time) compilation and when would you use it?",
          "answer": "<p><strong>AOT (Ahead-of-Time)</strong> compiles IL to native code at <em>build time</em> rather than at runtime — eliminating the JIT startup cost. .NET supports two AOT approaches: <strong>ReadyToRun (R2R)</strong> — partial AOT. Some methods are pre-compiled to native code at build time; JIT fills in the rest on first call. Reduces startup time without the restrictions of full AOT. <strong>Native AOT</strong> (.NET 7+) — full compilation to a native executable with no JIT, no IL, no CLR startup. Sub-millisecond startup times, smaller binary, lower memory. Restrictions: no runtime code generation (<code>Assembly.LoadFile</code>, some reflection patterns, <code>Activator.CreateInstance</code> without source generators). Best for: Lambda functions, microservices, CLI tools where startup speed matters. Not ideal for large enterprise apps with heavy reflection (EF Core, some serializers have limitations). Enable with <code>&lt;PublishAot&gt;true&lt;/PublishAot&gt;</code> in <code>.csproj</code>.</p>"
        },
        {
          "question": "What is the CLR and what services does it provide?",
          "answer": "<p>The <strong>CLR (Common Language Runtime)</strong> is the managed execution environment for .NET code. It sits between your compiled IL and the OS/hardware and provides: <strong>JIT compilation</strong> — IL → native code. <strong>Garbage Collection</strong> — automatic memory management, generational GC. <strong>Type safety</strong> — verifies IL is type-safe before execution. <strong>Exception handling</strong> — structured try/catch/finally across call stacks. <strong>Thread management</strong> — thread pool, synchronisation primitives. <strong>Security</strong> — Code Access Security (largely removed in .NET Core). <strong>Interop</strong> — P/Invoke (calling native C APIs), COM interop. The CLR is what makes different .NET languages (C#, F#, VB.NET) interoperable — they all compile to the same IL and run on the same runtime. In .NET Core/.NET 5+, the CLR is called <strong>CoreCLR</strong> and is open source on GitHub.</p>"
        }
      ],
      "tip": "<strong>Interview flow:</strong> C# → IL (build time, Roslyn) → Native code (runtime, RyuJIT). JIT = first-call compilation, cached thereafter. The warm-up cost is the only real downside. Native AOT eliminates JIT entirely for scenarios needing instant startup. CLR provides GC, type safety, threading, and interop on top of JIT.",
      "tags": ["oops", "jit", "clr", "compilation", "il", "aot", "performance"],
      "sortOrder": 20,
      "isActive": true
    },
    {
      "id": "oops-021",
      "category": "oops",
      "difficulty": "easy",
      "frequency": "high",
      "question": "What is a static class in C# and when would you use one?",
      "answer": "<p>A <strong>static class</strong> in C# is a class that:</p><ul><li>Cannot be instantiated — no <code>new StaticClass()</code></li><li>Cannot be inherited</li><li>All members must be <code>static</code></li><li>Implicitly sealed</li></ul><p>The compiler enforces these rules — it is a design contract saying \"this class is just a container for shared utilities.\"</p><pre><code class=\"language-csharp\">// Good use: utility / extension methods with no state\npublic static class StringExtensions\n{\n    public static string Truncate(this string value, int maxLength)\n    {\n        if (string.IsNullOrEmpty(value)) return value;\n        return value.Length &lt;= maxLength\n            ? value\n            : value[..maxLength] + \"…\";\n    }\n\n    public static bool IsValidEmail(this string email) =&gt;\n        !string.IsNullOrEmpty(email) &amp;&amp; email.Contains('@');\n}\n// Usage: \"Hello World\".Truncate(5); // \"Hello…\"\n\n// Good use: constants / configuration keys\npublic static class ClaimTypes\n{\n    public const string UserId = \"uid\";\n    public const string SubscriptionPlan = \"plan\";\n}\n\n// Common anti-pattern: static class with mutable state (testability nightmare)\npublic static class Config  // ❌ can't mock, can't test in isolation\n{\n    public static string ConnectionString { get; set; } = \"\";\n    public static bool IsDebug { get; set; }\n}</code></pre><p>In the CS_PM_Service, the <code>Config</code> class is a static class used as a global config holder — simple but untestable. The InterviewPrepPortal instead uses <code>IOptions&lt;T&gt;</code> with DI, which is properly testable.</p>",
      "followUps": [
        {
          "question": "What is the difference between a static class and a singleton?",
          "answer": "<p>A <strong>static class</strong> is a language construct — no instance ever exists, all methods are called on the type itself: <code>MyStaticClass.DoSomething()</code>. State (if any) is shared for the entire process lifetime. No interface can be implemented, so it cannot be mocked or substituted. A <strong>Singleton</strong> (either the design pattern or DI lifetime) is a regular class with a single instance: <code>_myService.DoSomething()</code>. It can implement interfaces and be substituted/mocked in tests. Use static class for: pure functions, extension methods, constants — things with no state or with immutable state. Use Singleton (via DI) for: services that are expensive to create and should be shared (DB connection pool wrapper, cached config, IMemoryCache) — register with <code>builder.Services.AddSingleton&lt;T&gt;()</code>. The CS_PM_Service used a static Config class — if it had used <code>IOptions&lt;T&gt;</code> with DI, it could have been mocked in unit tests.</p>"
        },
        {
          "question": "What are extension methods and what makes them special about static classes?",
          "answer": "<p>Extension methods let you add methods to existing types without modifying them or subclassing. They must be defined as <code>static</code> methods in a <code>static</code> class, with the first parameter prefixed by <code>this</code>: <code>public static string ToCamelCase(this string value)</code>. After adding a <code>using</code> for the namespace, call it as if it were an instance method: <code>\"HelloWorld\".ToCamelCase()</code>. LINQ is built entirely on extension methods — <code>Where</code>, <code>Select</code>, <code>OrderBy</code> are all extension methods on <code>IEnumerable&lt;T&gt;</code> in the <code>System.Linq</code> namespace. In InterviewPrepPortal, LINQ extension methods query question collections: <code>questions.Where(q =&gt; q.IsActive).OrderBy(q =&gt; q.SortOrder)</code>. You can add extension methods to sealed types, interfaces, and even <code>string</code> or <code>int</code> — though adding them to built-in types should be used sparingly to avoid confusing APIs.</p>"
        }
      ],
      "tip": "<strong>When to use static:</strong> Utility methods (no state, no side effects), extension methods, constants. <strong>When NOT to use static:</strong> Services that need DI, anything you'd want to mock in tests, anything with mutable shared state (thread-safety nightmares). Static class = no instance, no interface, no inheritance, no mocking.",
      "tags": ["oops", "static", "extension-methods", "csharp"],
      "sortOrder": 21,
      "isActive": true
    },
    {
      "id": "oops-022",
      "category": "oops",
      "difficulty": "medium",
      "frequency": "medium",
      "question": "What is a nested class in C# and when would you use one?",
      "answer": "<p>A <strong>nested class</strong> is a class defined inside another class. It has access to the outer class's private members, and its visibility can be controlled with access modifiers.</p><pre><code class=\"language-csharp\">public class QuestionService\n{\n    // Private nested class — an implementation detail of QuestionService\n    // Not visible outside QuestionService\n    private class CacheEntry\n    {\n        public List&lt;Question&gt; Questions { get; init; } = [];\n        public DateTime LoadedAt { get; init; } = DateTime.UtcNow;\n        public bool IsExpired =&gt; DateTime.UtcNow - LoadedAt &gt; TimeSpan.FromHours(24);\n    }\n\n    private CacheEntry? _cache;\n\n    public List&lt;Question&gt; GetQuestions(string categoryId)\n    {\n        if (_cache is null || _cache.IsExpired)\n            _cache = new CacheEntry { Questions = LoadFromDisk(categoryId) };\n        return _cache.Questions;\n    }\n}\n\n// Public nested class — commonly used for builder patterns and result types\npublic class QueryResult&lt;T&gt;\n{\n    public bool Success { get; init; }\n    public T? Data { get; init; }\n    public string? ErrorMessage { get; init; }\n\n    // Nested builder\n    public class Builder\n    {\n        private T? _data;\n        private string? _error;\n\n        public Builder WithData(T data) { _data = data; return this; }\n        public Builder WithError(string msg) { _error = msg; return this; }\n        public QueryResult&lt;T&gt; Build() =&gt;\n            new() { Success = _data != null, Data = _data, ErrorMessage = _error };\n    }\n}</code></pre>",
      "followUps": [
        {
          "question": "What is the difference between a nested class and an inner class?",
          "answer": "<p>In C#, all classes defined inside another class are called <strong>nested classes</strong> — there's no distinct \"inner class\" keyword. But conceptually (from Java terminology): an <strong>inner class</strong> has an implicit reference to an instance of the outer class (in Java: non-static nested class). In C#, nested classes are always independent of the outer class instance — they're more like static nested classes in Java. A C# nested class doesn't automatically have access to <em>instance</em> members of the outer class unless you pass a reference explicitly. It does have access to <em>private static</em> members. This is a common misconception. If you need the nested class to operate on the outer class's instance, pass <code>this</code> or specific members explicitly in the constructor.</p>"
        },
        {
          "question": "When is a nested class better than a separate class?",
          "answer": "<p>Use a nested class when: (1) <strong>It's an implementation detail</strong> — the helper class makes no sense outside the context of the outer class (e.g., an internal cache structure, a private state machine). Making it a private nested class prevents it from polluting the public namespace. (2) <strong>Strong coupling is intentional</strong> — the nested class and outer class are inseparable. A <code>LinkedList.Node</code> nested class only makes sense as part of <code>LinkedList</code>. (3) <strong>Builder/Factory patterns</strong> — <code>HttpRequest.Builder</code>, <code>AlertDialog.Builder</code> — tight association communicates they're part of the same API. Don't use nested classes just to reduce file count — two unrelated or independently reusable types should be in separate files. Prefer private nested classes (hide implementation) over public nested classes (which add API surface complexity).</p>"
        }
      ],
      "tip": "<strong>Most common real-world uses:</strong> Private helper/state types that only make sense in context of the outer class. Builder pattern where <code>MyClass.Builder</code> makes the association obvious. Iterator/enumerator implementations (<code>IEnumerator&lt;T&gt;</code> inside a custom collection). Rarely needed in typical business code — most devs encounter nested classes more when reading framework source than when writing application code.",
      "tags": ["oops", "nested-class", "csharp", "encapsulation"],
      "sortOrder": 22,
      "isActive": true
    },
    {
      "id": "oops-023",
      "category": "oops",
      "difficulty": "easy",
      "frequency": "high",
      "question": "What is the difference between List<T>.Add() and List<T>.Insert()? What are their time complexities?",
      "answer": "<p>Both add an element to a <code>List&lt;T&gt;</code>, but in different positions with very different performance characteristics.</p><table><tr><th></th><th>Add(T item)</th><th>Insert(int index, T item)</th></tr><tr><td><strong>Position</strong></td><td>Always appends to the end</td><td>Inserts at the specified index</td></tr><tr><td><strong>Time complexity</strong></td><td>O(1) amortised</td><td>O(n) — shifts all elements after index right</td></tr><tr><td><strong>Use when</strong></td><td>Building a list (most cases)</td><td>Order matters and you need to insert in the middle</td></tr></table><pre><code class=\"language-csharp\">var questions = new List&lt;string&gt;(capacity: 10); // pre-allocate for O(1) adds\n\n// Add() — appends to end — O(1) amortised\nquestions.Add(\"What is DI?\");       // [\"What is DI?\"]\nquestions.Add(\"What is SOLID?\");    // [\"What is DI?\", \"What is SOLID?\"]\n\n// Insert() — shifts existing items right — O(n)\nquestions.Insert(0, \"What is OOP?\"); // inserts at index 0, shifts everything\n// [\"What is OOP?\", \"What is DI?\", \"What is SOLID?\"]\n\n// Insert at middle — O(n) because half the list must shift\nquestions.Insert(1, \"What is LINQ?\");\n// [\"What is OOP?\", \"What is LINQ?\", \"What is DI?\", \"What is SOLID?\"]\n\n// Add() amortised O(1) — List doubles capacity when full\n// [0, 0, 0, 0] cap=4 → Add → [item, 0, 0, 0] cap=4  O(1)\n// [a, b, c, d] cap=4 → Add → [a, b, c, d, e, 0, 0, 0] cap=8  O(n) resize\n// But resizes happen O(log n) times total =&gt; amortised O(1) per add</code></pre>",
      "followUps": [
        {
          "question": "When should you use LinkedList<T> instead of List<T>?",
          "answer": "<p>Use <code>LinkedList&lt;T&gt;</code> when you frequently <strong>insert or remove from the middle</strong> of the collection AND you have a reference to the node: <code>AddAfter(node, value)</code> and <code>Remove(node)</code> are O(1) — no shifting required. Trade-off: <code>LinkedList&lt;T&gt;</code> has no random access by index (no <code>list[i]</code>) — finding the 5th element requires traversal from the head: O(n). It also uses more memory (each node stores previous + next pointers) and is cache-unfriendly (nodes scattered in memory vs List's contiguous array). In practice, <code>List&lt;T&gt;</code> outperforms <code>LinkedList&lt;T&gt;</code> in most real-world scenarios because modern CPUs heavily optimise sequential memory access (cache lines). Use <code>LinkedList&lt;T&gt;</code> only for: LRU cache implementation, music playlist (insert/remove tracks by reference), undo/redo queues. For most CRUD operations on collections, <code>List&lt;T&gt;</code> is the right default.</p>"
        },
        {
          "question": "What is the difference between List<T>, IList<T>, IEnumerable<T>, and ICollection<T>?",
          "answer": "<p>These are an interface hierarchy: <code>IEnumerable&lt;T&gt;</code> → <code>ICollection&lt;T&gt;</code> → <code>IList&lt;T&gt;</code> → <code>List&lt;T&gt;</code>. <strong>IEnumerable&lt;T&gt;</strong> — read-only, forward-only iteration. The minimum interface: only provides <code>GetEnumerator()</code>. Use for method parameters when you only need to iterate. <strong>ICollection&lt;T&gt;</strong> — adds Count, Add, Remove, Contains, Clear. Use when you need to know the count or modify the collection but don't need indexing. <strong>IList&lt;T&gt;</strong> — adds random access by index (<code>this[int]</code>), Insert, RemoveAt. <strong>List&lt;T&gt;</strong> — concrete implementation with additional methods (Sort, BinarySearch, FindAll, Capacity). Rule: declare method parameters and return types as the most restrictive interface that satisfies the need. Take <code>IEnumerable&lt;T&gt;</code> if you only iterate; take <code>IList&lt;T&gt;</code> if you need indexed access. This makes code more flexible — callers can pass arrays, List, or any IEnumerable.</p>"
        }
      ],
      "tip": "<strong>Rule:</strong> Always use <code>Add()</code> when building a list. Only use <code>Insert()</code> when position within the list truly matters — remember it's O(n). If you find yourself calling <code>Insert(0, ...)</code> frequently (prepend), consider a <code>Queue&lt;T&gt;</code> or reversing your build order. Pre-allocate with <code>new List&lt;T&gt;(capacity)</code> when you know the approximate size to avoid repeated resizes.",
      "tags": ["oops", "list", "collections", "csharp", "time-complexity"],
      "sortOrder": 23,
      "isActive": true
    }
  ]
}
