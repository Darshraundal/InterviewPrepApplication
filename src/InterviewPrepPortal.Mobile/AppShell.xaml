<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:pages="clr-namespace:InterviewPrepPortal.Mobile.Pages"
       Title="Interview Prep"
       x:Class="InterviewPrepPortal.Mobile.AppShell"
       BackgroundColor="{StaticResource SurfaceDark}"
       Shell.FlyoutBehavior="Disabled">

    <TabBar>
        <ShellContent Title="Home" Route="home" ContentTemplate="{DataTemplate pages:HomePage}" />
        <ShellContent Title="Categories" Route="categories" ContentTemplate="{DataTemplate pages:CategoriesPage}" />
        <ShellContent Title="Projects" Route="projects" ContentTemplate="{DataTemplate pages:ProjectsPage}" />
        <ShellContent Title="Search" Route="search" ContentTemplate="{DataTemplate pages:SearchPage}" />
        <ShellContent Title="Progress" Route="progress" ContentTemplate="{DataTemplate pages:ProgressPage}" />
    </TabBar>

</Shell>
