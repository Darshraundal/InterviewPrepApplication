<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="InterviewPrepPortal.Mobile.Pages.NotesPage"
             Title="Notes">

    <RefreshView IsRefreshing="{Binding IsBusy}" Command="{Binding LoadCommand}">
        <CollectionView ItemsSource="{Binding Notes}" Margin="16">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Frame Style="{StaticResource CardFrame}" Margin="0,6">
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnQuestionTapped" />
                        </Frame.GestureRecognizers>
                        <VerticalStackLayout Spacing="6">
                            <Label Text="{Binding QuestionText}" FontAttributes="Bold" />
                            <Label Text="{Binding NoteText}" Style="{StaticResource SubtleLabel}" />
                            <Button Text="Delete" HorizontalOptions="End" Clicked="OnDeleteTapped" />
                        </VerticalStackLayout>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </RefreshView>

</ContentPage>
